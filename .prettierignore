.next
node_modules
package-lock.json
public
docs/specs

# python harnesses (uv-managed; formatted by ruff conventions, not prettier)
tools/

# drizzle-kit generates these; formatting them fights the tool on every migration
src/db/migrations/meta
