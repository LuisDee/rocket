# Response ledger: adversarial review of 2026-09-06

**Answers:** [`docs/reviews/2026-09-06-adversarial-review.md`](2026-09-06-adversarial-review.md) (F1-F34, section 6 gaps S6.1-S6.10, section 9 housekeeping bullets H1-H3).
**Written:** 2026-09-07. **Branch:** `feat/m1-core-loop` at `b026fb9`, eight commits past the `4d68b7c` the review read. **Race:** 2026-10-24, 47 days out.
**Method:** five verifiers re-checked every finding against the branch as it stands, not as the review found it. Where a finding's evidence has gone stale, the ledger says so and restates the evidence rather than carrying the review's numbers forward.

---

## Verdict

The review is right about the training and about the contracts, and wrong about roughly a fifth of its evidence -- which is what you would expect from a document written against a branch that moved 1,122 lines of product code in the twenty-four hours after it was written. What it got right and nobody else would have: **F6, three entered races missing from the re-derived macro layer**, which had put the block's peak 35 km long run on Lincoln Half day and a cut-back long run on 10K day, with nothing in the repo able to catch it because races were not data anywhere the planner could see. That is the single most important thing in the review: a training defect, already shipped, found by reading the calendar against the config rather than by reading code. It is now fixed in `b6ea990`. Close behind it, **F17** (Strava's API policy since 2026-06-01 prohibits API data in any AI application -- verified against the primary source, not the appendix) turns 174 lines of working probe into a licence liability, and **F28** (every write tool returns `{applied, violated_rules[], compliant_alternative, resulting_window}`, validated over the whole rolling window) is the cheapest high-value item in the whole document: spec text and a return type, authored before a single tool exists, and the seam that makes "discussions with Claude shift my training" safe rather than merely conversational.

Where it is wrong, it is wrong in a consistent way: it asserts facts about the repo that were true at `4d68b7c` and states them as if they were structural. **F1's headline "zero product code" is stale by eight commits.** F5 claims `CLAUDE.md` carries the 3:25-3:35 band; it does not, and never did. F8's weeks-3-and-4 `UNRATIFIED` exemptions were replaced wholesale by `8385910` before the review's ink dried. F13 reads a musculoskeletal multiplier (`swimMusculoskeletal: 0.05`) as a total-cost one and concludes the spec undercounts swim cardio; it does not. F7's ratio table is computed against a 22/26/32 km ladder that no longer exists -- the real block breaches harder than the review calculated. F22 says the plan is wrong that the MCP SDK went v2; npm says both are half right, and the plan's actual error is elsewhere (`mcp-handler` was not superseded, it is at 2.1.1 on v2). F20 is wrong about where the planner runs, and acting on it would have justified not persisting availability at all. One finding is rejected outright.

**On statuses.** The review permits four. A fifth, `ALREADY-DONE`, is used for six findings whose exact remedy landed before this ledger was written (F21, F24, F30, F34, H1, H2) -- calling those `ACCEPT` would imply work outstanding, and `REJECT` would imply the finding was wrong. **No finding carries `NEEDS-LUIS` at the top level**, because all four section-8 questions were answered on 2026-09-07: races ratified (Dorney dropped, Lincoln and LDNX stand), intervals.icu primary with the Garmin probe as manual fallback, goal as a range derived from the 12 September half with "as fast as possible" retired, and scope option A. Two narrow decisions remain and are listed in section 4.

---

## The findings

| #   | Claim, in one line                                                               | Status          | Detail      |
| --- | -------------------------------------------------------------------------------- | --------------- | ----------- |
| F1  | Zero product code 48 days out                                                    | ACCEPT-MODIFIED | [F1](#f1)   |
| F2  | Observed velocity does not deliver the ten-stage plan before the race            | ACCEPT-MODIFIED | [F2](#f2)   |
| F3  | Branch is behind main, re-introduces reverted spec content, drops LICENSE/README | ACCEPT          | [F3](#f3)   |
| F4  | Concurrent-agent commit provenance already broken once                           | ACCEPT-MODIFIED | [F4](#f4)   |
| F5  | Goal moved to a 3:50-4:00 band; top-level docs still carry the old number        | ACCEPT-MODIFIED | [F5](#f5)   |
| F6  | Three fixed races vanished from the re-derived macro layer                       | ACCEPT-MODIFIED | [F6](#f6)   |
| F7  | Ramp guardrail is on the wrong axis; the block breaches the right one            | ACCEPT-MODIFIED | [F7](#f7)   |
| F8  | Unratified thresholds are driving the plan                                       | ACCEPT-MODIFIED | [F8](#f8)   |
| F9  | Four long runs, longest 20 days out -- correctly stated, no action               | ACCEPT-MODIFIED | [F9](#f9)   |
| F10 | The "block collapsed" error is evidence for deterministic aggregation            | ACCEPT-MODIFIED | [F10](#f10) |
| F11 | Deterministic planner as sole writer is right                                    | ACCEPT-MODIFIED | [F11](#f11) |
| F12 | Readiness over-trusts the watch (40% objective share, raw daily terms)           | ACCEPT          | [F12](#f12) |
| F13 | Late swims corrupt the following morning's readiness inputs                      | ACCEPT-MODIFIED | [F13](#f13) |
| F14 | TSB must never gate a session on its own                                         | ACCEPT          | [F14](#f14) |
| F15 | Taper, quality-session count, context multipliers, half-to-marathon ratio        | ACCEPT-MODIFIED | [F15](#f15) |
| F16 | The two-component load model is correctly deferred                               | ACCEPT          | [F16](#f16) |
| F17 | Strava's API policy prohibits API data in any AI application                     | ACCEPT          | [F17](#f17) |
| F18 | intervals.icu is the sanctioned Garmin path; Branch A was under-weighted         | ACCEPT-MODIFIED | [F18](#f18) |
| F19 | The plan is never on the wrist                                                   | ACCEPT-MODIFIED | [F19](#f19) |
| F20 | Google Calendar is already a connector; ICS ingest is unnecessary                | **REJECT**      | [F20](#f20) |
| F21 | Garmin bulk export remains the right backfill source                             | ALREADY-DONE    | [F21](#f21) |
| F22 | Do not chase SDK v2; use DoHardThings' proven stack                              | ACCEPT-MODIFIED | [F22](#f22) |
| F23 | Do not build an OAuth server first for a single user                             | ACCEPT-MODIFIED | [F23](#f23) |
| F24 | PWA: manifest plus a hand-written service worker, no Serwist                     | ALREADY-DONE    | [F24](#f24) |
| F25 | Rocket stays a separate repo; lift six named assets from DoHardThings            | ACCEPT-MODIFIED | [F25](#f25) |
| F26 | Capability packs: browse directly, proxy only the state-changing seams           | ACCEPT-MODIFIED | [F26](#f26) |
| F27 | Chat statements that affect the plan are lost unless written                     | ACCEPT-MODIFIED | [F27](#f27) |
| F28 | Propose/validate contract on every write tool, whole-window validation           | ACCEPT          | [F28](#f28) |
| F29 | Evals: vitest fixtures first, a handful of promptfoo cases second                | ACCEPT          | [F29](#f29) |
| F30 | Two honest options for the 48 days; recommends A                                 | ALREADY-DONE    | [F30](#f30) |
| F31 | What "strong foundation" should mean here                                        | ACCEPT-MODIFIED | [F31](#f31) |
| F32 | The earlier attempt: rocket must not ship as another static viewer               | ACCEPT-MODIFIED | [F32](#f32) |
| F33 | `03-planner.md:11` still carries the personal place name main removed            | ACCEPT          | [F33](#f33) |
| F34 | Confirm the probe caches are gitignored                                          | ALREADY-DONE    | [F34](#f34) |

**Completeness.** F1 to F34 is thirty-four findings and all thirty-four are dispositioned above. **None is UNADDRESSED.** Section 9 of the review contains three unnumbered bullets after F33 and F34, not the four a first reading suggests; they are carried below as H1-H3. Section 6's ten consolidated gaps are carried as S6.1-S6.10 -- four of them (S6.3, S6.4, S6.8, and half of S6.6) are duplicates of numbered findings and are marked as such so they are not tracked twice.

| #     | Claim, in one line                                                          | Status                       | Detail         |
| ----- | --------------------------------------------------------------------------- | ---------------------------- | -------------- |
| H1    | homescreen-app task file unchecked while its files sit uncommitted          | ALREADY-DONE                 | [H1](#h1)      |
| H2    | Confirm `ci:harness-tests` exists in the branch's check workflow            | ALREADY-DONE                 | [H2](#h2)      |
| H3    | `~/dev/routr/artifacts/fit_crop/` is the only local FIT history; back it up | ACCEPT                       | [H3](#h3)      |
| S6.1  | Missing: single-session spike guardrail (F7)                                | ACCEPT                       | [S6.1](#s61)   |
| S6.2  | Missing: workouts pushed to the watch (F19)                                 | ACCEPT                       | [S6.2](#s62)   |
| S6.3  | Missing: `notes` table and `rocket_add_note` (F27)                          | ACCEPT-MODIFIED (dup of F27) | [S6.3](#s63)   |
| S6.4  | Missing: applied/rejected/alternative contract (F28)                        | ACCEPT (dup of F28)          | [S6.4](#s64)   |
| S6.5  | Missing: proactivity -- daily cron plus iOS Web Push                        | ACCEPT-MODIFIED              | [S6.5](#s65)   |
| S6.6  | Missing: goal as a range, surfaced in `get_status` (F5)                     | ACCEPT-MODIFIED              | [S6.6](#s66)   |
| S6.7  | Missing: swim end-time as a readiness input (F13)                           | ACCEPT-MODIFIED              | [S6.7](#s67)   |
| S6.8  | Missing: race-added and race-cancelled fixtures (F29)                       | ACCEPT (dup of F29)          | [S6.8](#s68)   |
| S6.9  | Missing: a rules registry returned as `applied_rules[]`                     | ACCEPT-MODIFIED              | [S6.9](#s69)   |
| S6.10 | Missing: under- and over-performing detection                               | ACCEPT                       | [S6.10](#s610) |

---

## Detail

<a id="f1"></a>

### F1. Zero product code 48 days out -- ACCEPT-MODIFIED

**Verification.** The literal claim is now false. `src/db/schema.ts` (222 lines), `src/db/client.ts` (73), `src/db/ingest-schema.ts` (83), `src/db/migrations/0000_init.sql` (103) and `0001_append_only_guards.sql` (110), `src/app/page.tsx` (215), `src/lib/block.ts` (124) and `src/lib/block.test.ts` (105) are committed -- 1,122 lines, landed in `866f7b6` and `9153fde`. The substance survives: `ls src/lib src/db` shows no planner, no load engine, no ingest, and `package.json` dependencies are `@vercel/functions`, `drizzle-orm`, `next`, `pg`, `react`, `react-dom` -- no MCP SDK, so no server. `9153fde`'s own body says the DB layer is "not yet exercised against the live database", and `src/app/page.tsx` still renders off a committed JSON snapshot.

**The other side.** The finding's own counter -- scaffold and probe are genuine de-risking -- is the weaker half. The stronger one, which the review does not make, is that F1 is not a finding at all: its Action reads "No dispute about quality. The dispute is ordering and volume of ceremony against the clock; see F30 to F32." It asserts a fact and defers every consequence, which makes it unfalsifiable by design -- whatever the code count, the action is elsewhere. That argues for reject-as-non-finding. It does not win, because the fact it asserts is the premise F30-F32 and Luis's option-A decision rest on, and that premise moved from 0 to 1,122 lines in a single day.

**Justification.** Accept the substance, reject the headline. The honest restatement is: the persistence layer and a static home screen exist and are untested against live infrastructure; the coach itself -- load engine, planner, MCP server, check-in, push -- does not. That is still the right alarm at 47 days, and it is exactly what option A answers. No independent action falls out of F1 that F30-F32 do not already carry.

**Action.** None as a standalone item. This row exists so downstream readers do not carry the "zero" figure forward as live.

<a id="f2"></a>

### F2. Velocity does not support the plan as written -- ACCEPT-MODIFIED

**Verification.** The calendar evidence checks out -- `git log` on main shows `43fd391`/`08cdf28` then a three-week gap to 2026-09-06. The velocity premise has since been falsified: eight commits landed between `4d68b7c` and `b026fb9` inside roughly a day, including the whole of Stage 1 (`9153fde`) and the home screen (`866f7b6`). Stage ordering confirmed at `docs/plans/PLAN-2026-001-m1-core-loop.md:259` (Stage 0), `:282` (Stage 1), `:450` (Stage 5, OAuth), `:483` (Stage 6, planner) -- the planner is indeed behind the auth server. The finding's requested action, a realistic day-count per stage, is **not** in the plan: grepping for day/hour/estimate against stage headers returns prose only, no budget.

**The other side.** Stages 0-5 are each small for an agent and Stage 1 took under a day of wall clock, so F2's premise arguably measured a holiday rather than a build rate -- which would make this a REJECT. It does not win: the throughput came from many parallel agents on one branch, and that same parallelism produced F4's provenance breakage and two temporarily red task-trace checks. Velocity bought at the cost of traceability does not extrapolate. And Stage 5 sitting ahead of Stage 6 is a sequencing error independent of anyone's build rate.

**Justification.** The ordering half is right and is already being actioned by option A. The velocity half is stale and, on the evidence, wrong. The unmet piece is the day-count -- and with scope already cut, a full ten-stage budget is waste; what is needed is a budget for the surviving stages only.

**Action.** Do not build the ten-stage day-count. When the plan is rewritten for option A, put a one-line wall-clock budget against each surviving stage and a single "if not done by `<date>`, cut X" line. Mark Stage 5 and the other deferred items as out of the pre-race plan inside the plan document -- it still reads as ten stages.

<a id="f3"></a>

### F3. The branch is behind main and re-introduces reverted content -- ACCEPT

**Verification.** Unchanged and still true. `git log HEAD..main` returns `65fb66b` and `64cea56`. `git diff --stat main..HEAD -- LICENSE README.md` shows `LICENSE | 21 -` and `README.md | 45 -`, and neither file exists in the worktree. The branch's `vitest.config.mts` still reads `exclude: ['node_modules/**', '.next/**']` -- it lacks `64cea56`'s worktree exclusion, which is the fix for vitest reaching into `.worktrees/` and failing pre-push on another branch's files.

**The other side.** The review says "a rebase or merge of main fixes it mechanically". That is demonstrably wrong: `git merge-tree --write-tree main HEAD` produces `CONFLICT (content)` in **both** `docs/specs/01-domain-model.md` and `docs/specs/03-planner.md` -- exactly the two files where resolution is a judgement. `03-planner.md` is the run-commute line (F33); `01-domain-model.md` is the race list, which `b6ea990` has just rewritten. A naive `git merge -X theirs` either resurrects the personal place name or clobbers the live race rework.

**Justification.** Accepted on all three legs and upgraded in urgency, because the branch is getting further from main rather than closer -- the merge now conflicts where it may not have when the review was written. The vitest fix is the sharpest cost: `64cea56` exists because that exact bug already blocked a push once, and the branch is still exposed to it.

**Action.** Merge main into `feat/m1-core-loop` before the next feature commit lands, resolving both conflicts by hand: take main's generalised run-commute wording in `03-planner.md` (drops the place name, records the 2026-08-18 disable ratification), and in `01-domain-model.md` take the branch's `RACES`-backed structure while deleting the Dorney line, which Luis dropped on 2026-09-07. Coordinate with `race-aware-block`, which owns the race list.

<a id="f4"></a>

### F4. Concurrent-agent commit provenance is already broken once -- ACCEPT-MODIFIED

**Verification.** The incident is real and honestly recorded: `tasks/garmin-hardening.md:63-71` states the whole diff landed inside a concurrent agent's `git add -A`, and `4d68b7c` corrected the attribution rather than rewriting history. The requested process rule does **not** exist -- grepping `AGENTS.md` and `docs/STARTUP_ACCESS.md` for `git add|worktree|concurrent|provenance|parallel` returns nothing. The hazard recurred in a second form: `check_task_trace.py --range main..HEAD` was red on `6d4dd48` and `9153fde`, whose task files did not list them. Both have since been recorded (`d8d9cc1`, `b026fb9` and siblings); the gate is green at the time of writing.

**The other side.** Recorded honestly, history not rewritten, correct call -- not disputed. The stronger argument against acting is that the proposed rule ("one agent per worktree, or `git add <paths>` only") is the kind of process doc that gets written and ignored, while the repo already has a mechanical enforcer in `check_task_trace.py` that fails closed at pre-push. That half-wins: the gate catches the trailer/task-file mismatch, the recoverable symptom. It does not catch the actual failure mode -- a correct trailer on a commit whose diff belongs to someone else, which is what `758a2dc` was and which no automated check can see.

**Justification.** Accept the finding, shrink the action. A prose rule is worth one line, not a section, because it is cheap and the failure it prevents already cost a correction commit. The gate half of the original action has resolved itself since the dispositions were taken.

**Action.** One line in `AGENTS.md` section 3: agents stage with explicit paths, never `git add -A`, when another agent may be working the same worktree, with `(Discovered 2026-09-06: 758a2dc)` appended per AGENTS.md rule 6. The routr twelve-worktree note is out of scope -- routr is read-only with an active agent.

<a id="f5"></a>

### F5. The goal moved and the top-level docs do not know -- ACCEPT-MODIFIED

**Verification.** `docs/specs/06-training-block.md:16` still reads "Implied goal band ~3:25-3:35; provisional until Lincoln Half (rehearsal) confirms". The SUPERSEDED banner at `:5-12` is scoped "as to dates and volumes" only, so it does **not** cover line 16 -- the stale band is live text. `config/training.ts` `PACE_ESTIMATES.planningBandSeconds` is `{fast: 13800, slow: 14400}` = 3:50-4:00. The review's other evidence claim is **false**: `CLAUDE.md` never carried the 3:25-3:35 band; its only goal line says "as fast as possible". `MEASURED_BASE.weeklyKm` averages 28.1 km over eleven recorded weeks, matching Appendix B Q8's basis. The Battersea Half is 2026-09-12, five days out and not yet run, so no band can be derived from a result today.

**The other side.** The review's counter is that the config already defers the goal to the 12 September half, so the docs are stale rather than wrong in intent. That defence is weaker than it looks: `06-training-block.md:16` names **Lincoln** as the settling event, and Lincoln's role has changed twice -- it is now a marathon-pace rehearsal, and the 12 September Battersea Half took the pace-setting job. So the stale line does not merely carry an old number, it points a future reader at the wrong race for the derivation. That is precisely the failure mode F10 records. Separately, Luis retired "as fast as possible" as a planning input on 2026-09-07, which makes `CLAUDE.md`'s goal line newly wrong for a reason the review did not identify.

**Justification.** Substance right, evidence partly wrong, action splits on the calendar. Two documents are stale and must be corrected now. The claim about `CLAUDE.md`'s contents is rejected as written -- the error there is different and larger. The range-derivation half is not blocked on judgement, it is blocked on a race five days out; recording the derivation **rule** is what is actionable today.

**Action.** (1) Replace `docs/specs/06-training-block.md:16` with a pointer to `PACE_ESTIMATES.planningBandSeconds` and name the Battersea Half, not Lincoln, as the settling event; widen the supersession banner at `:5-12` to cover the athlete-baseline section. (2) Amend `CLAUDE.md`'s goal line to drop "as fast as possible" and state the goal is a range derived from the 12 September half. (3) After 12 September, re-derive `planningBandSeconds` from the actual result using the 2.11-2.15 recreational half-to-marathon multiplier (Appendix B Q6) rather than the Garmin-derived 1.135 Riegel exponent, and record it as a decision entry.

<a id="f6"></a>

### F6. Three fixed races vanished from the re-derived macro layer -- ACCEPT-MODIFIED

**Verification.** The bug was real and is now fixed. `config/training.ts` records it in the header above `RACES` ("placed the peak 35 km long run on Sunday 4 October -- Lincoln Half day ... Nothing caught it, because BLOCK held only the goal and tune-up races"). A `RACES` array and a derived `LIVE_RACE_DATES` now exist, with Dorney retained as `role: 'dropped'` carrying Luis's 2026-09-07 ratification. Every `BLOCK_WEEKS` entry now carries `longRunDate`, with `longRunOnRace` naming the race where one carries the session -- weeks 4 and 5 included, which were still incomplete when the dispositions were taken. `config/training.test.ts` imports both symbols and asserts the six long sessions with three carried by races. Landed in `b6ea990`.

**The other side.** The counter that matters is the reshape's own, in `tasks/race-aware-block.md`: moving long runs to the Saturdays before each race is rejected because it stacks 30 km+ the day before a half and a 10K, and dropping the peak long run is rejected because it leaves nothing over 30 km. Both rejections are correct. The delivered reshape resolves it the third way -- extend the race day itself: Lincoln at marathon pace inside a ~35 km day, LDNX hard inside ~16 km. That is standard Hansons/Daniels practice and satisfies both rejections at once, and it is reversible if Lincoln is raced hard rather than run at marathon pace.

**Justification.** The data shape is right and minimal: `RACES` as a flat array, dropped rows retained rather than deleted (a released race is a decision, not an absence), `LIVE_RACE_DATES` derived rather than duplicated, and additive fields on `BLOCK_WEEKS` so `src/lib/block.ts` and `src/app/page.tsx` keep working. The week-4 construction that the dispositions flagged as wrong -- Lincoln replacing the 35 km rather than carrying it -- was corrected in the delivered commit, so the F9 constraint survives.

**Action.** Delivered by `b6ea990`. Remaining: delete the three races from `docs/specs/01-domain-model.md:8-10` as an authoritative source and point that section at `RACES`, resolved as part of the F3 merge.

<a id="f7"></a>

### F7. The ramp guardrail is on the wrong axis -- ACCEPT-MODIFIED

**Verification.** The axis claim holds against Appendix B Q2: Nielsen 2025 BJSM 59:1203 (Garmin-RUNSAFE, 5,205 runners, ~600k sessions) -- hazard rises above 110% of the trailing-30-day longest (HRR 1.64 / 1.52 / 2.28), week-to-week ratios not significant, ACWR inversely associated; Damsted 2018 rates weekly-load evidence "very limited". No single-session guardrail exists: grepping `config/training.ts` for `spike|trailing|30-day` returns nothing. The review's claim that weekly km is the _only_ ramp guardrail is partly stale -- `8385910` added `highVolumeThresholdKm: 80` / `minRunDaysAtHighVolume: 6`, a session-density guardrail on the right axis, though not the Nielsen rule. The review's ratio table is computed against a 22/26/32 km ladder that no longer exists. Recomputed against the committed block using `src/data/recent-activities.json` (30 runs, 2026-06-24 to 2026-09-05; the 16.0 km of 22 August is confirmed): 12 Sep 21.1 vs 16.0 = **132%**; 19 Sep 20 vs 21.1 = 95%; 27 Sep 30 vs 21.1 = **142%**; 4 Oct 35 vs 30 = **117%**; 11 Oct 26 vs 35 = 74%; race day 42.195 vs 35 = **121%**. The block breaches harder than the review calculated, and the worst breach is the 21.1 -> 30 km step on 27 September, not the peak long run.

**The other side.** A population hazard ratio is not a hard cap, and a man with a 31.5 km run 34 days ago is not the modal novice. Two things sharpen that and one blunts it. Sharpening: the goal race itself is 121-141% of the trailing-30-day longest under **every** constructible version of this block -- a guardrail the goal race cannot pass is a warning, not a cap; and the hazard ratios are **non-monotonic** (1.64 for 10-30%, 1.52 for 30-100%, 2.28 for >100%), so smoothing 27 September from 142% to 123% moves it out of the 1.52 band and _into_ the 1.64 band, buying nothing by the paper's own point estimates. Only the binary (over 110% or not) carries usable signal. Blunting: the 31.5 km run of 9 August is the run that wrecked him for a week -- evidence the hazard is real for him, not that he is immune, though it does establish 30 km is not a novel distance for his tissue.

**Justification.** Accept the axis, reject the form. Nielsen 2025 is the largest prospective dataset on the question and directly nulls the week-to-week ratio the config guards on; adding the measure is right and cheap. But "primary guardrail" at 110% cannot mean a cap, because the marathon breaches it by construction. It belongs in the negotiate-never-break shape `docs/specs/03-planner.md:28` already mandates: compute the ratio, name the breach, cost it, let it be overridden, log the override. The appendix's paired recommendation to drop the weekly cap to 10% is **rejected** -- Luis ratified an aggressive rebuild on 2026-09-06, and re-imposing 10% would reverse a ratified decision through a config edit rather than a conversation.

**Action.** Add `GUARDRAILS.singleSessionSpikePct = 110`, measured against the longest **completed** run in the trailing 30 days, as a costed advisory and never a blocker, with sessions on `LIVE_RACE_DATES` exempt by declaration now that `RACES` makes that computable. Keep both weekly caps. Separately and independently: reshape the long-run ladder from 20/30/35 to 22/27/33 -- 19 Sep 22 (104%), 27 Sep 27 (123%), 4 Oct 33 (122%), race day (128%). Weekly totals unchanged, no session above ~130%, and race day drops from 121% to 128% under the committed shape. The 21.1 -> 30 km step is the block's one genuinely avoidable spike and costs nothing to remove.

<a id="f8"></a>

### F8. Unratified thresholds are driving the plan -- ACCEPT-MODIFIED

**Verification.** The specific evidence is **false** as of `8385910`, which replaced the entire `BLOCK_WEEKS` the review read. The weeks-3-and-4 `UNRATIFIED` exemptions no longer exist: the only non-null `rampExemption` in the block is week 2's, which reads "RATIFIED by Luis 2026-09-06, explicitly and after the cost was stated", and `config/training.test.ts` asserts that marker. What remains true: `GUARDRAILS.returningFromRestRampCapPct: 35` retains its PROVISIONAL docstring, and `docs/decisions.md` still closes the 2026-08-15 entry "PROVISIONAL pending Luis ratifying the returning-from-rest rule". But grepping `returningFromRestRampCapPct` across `*.ts` returns exactly two hits outside its own definition, both in `config/training.test.ts`, one of which asserts the block **exceeds** it. Nothing consumes the value.

**The other side.** "They are flagged loudly; that is the system working" is right, and argues for a different disposition than the review reaches. The review routes this to NEEDS-LUIS and blocks Stage 6's ramp logic until 35 is ratified. That blocker is dead: week 2's step is +72.4% on the 34.8 km pre-taper base, which exceeds 35 as well as 15, so the block's one override stands on Luis's explicit ratification and nothing else. The number 35 drives nothing. Asking Luis to ratify a threshold no code path reads, five days before his tune-up race, spends one of his scarce decisions on nothing.

**Justification.** Accept that the config and the decision log are stale; **reject the NEEDS-LUIS routing and reject the planner blocker**. The finding's premise -- unratified thresholds _driving_ the plan -- was true when written and is false now: the plan is driven by an explicitly ratified override, and the unratified number survives only as dead config with a test pointing at it. The lazy and correct fix is deletion, not ratification: it closes an open PROVISIONAL decision, removes a threshold that could silently start driving something later, and shrinks the diff. If the returning-from-rest concept is wanted for a future block, it comes back when a planner needs it and can be ratified against a real case.

**Action.** Delete `GUARDRAILS.returningFromRestRampCapPct` and rewrite the test that asserts against it to assert the week-2 rise against `ACTIVE_RAMP_CAP_PCT` plus the RATIFIED marker -- the assertion that matters. Close the `docs/decisions.md` 2026-08-15 "ramp cap contradiction, unresolved" entry as SUPERSEDED by the 2026-09-06 aggressive-mode entry, stating that the seed block the placeholder existed to make expressible no longer exists. Remove the Stage 6 planner blocker in PLAN-2026-001 that cites it. No Luis decision required.

<a id="f9"></a>

### F9. Only four long runs, longest 20 days out -- ACCEPT-MODIFIED

**Verification.** Correct as written against the state the review read: long runs were 20 / 30 / 35 / 26, longest 35 km on 4 October, 20 days before race day, stated plainly at `docs/plans/PLAN-2026-001-m1-core-loop.md:6`. Doherty 2020 basis confirmed at Appendix B Q5 and Q8 -- longest run and the count of 32 km+ runs both associate with faster marathons, and a sub-65 km peak week is the "low" plan category. `config/training.test.ts` now asserts six long sessions, three carried by races.

**The other side.** The defence of a Lincoln-carries-week-4 reshape is that Hansons never exceeds 26 km and produces fast marathons, so 30 km at 27 days plus a marathon-pace half at 20 days is a defensible marathon-specific stimulus. It does not transfer: Hansons compensates for the short long run with sustained 100+ km weeks over months of cumulative fatigue, and this block has four build weeks off a 28 km/week base. Without the chronic volume that makes the short long run work, cutting the peak long run removes the stimulus without supplying the substitute.

**Justification.** The finding was accurate, but its "Action: none" was not safe, because the fix for F6 was in the process of degrading the exact constraint F9 names -- the classic shape of a silent regression, two findings actioned independently with one quietly undoing the other. The delivered reshape avoided it by extending Lincoln into a ~35 km day rather than letting it replace the long run. F9's residual value is as the standing check on any further reshape.

**Action.** Promote F9 from "recorded, no action" to an acceptance criterion on `tasks/race-aware-block.md`: no reshape may reduce the block's longest single run below ~32 km, and each week's note must state its longest run and its days-out explicitly so the degradation cannot happen silently. Combined with F6 and F7 the target shape is 22 / 27 / 33 (Lincoln at marathon pace plus easy) / LDNX 10K.

<a id="f10"></a>

### F10. The "block collapsed" error is evidence for the architecture -- ACCEPT-MODIFIED

**Verification.** The error and its correction are recorded honestly at `docs/decisions.md:375-397`, status ACTIVE, including the withdrawn recommendation and Garmin's acute:chronic 1.00 as the independent cross-check. But the lesson recorded there is **procedural** ("Aggregate from the full series before drawing a conclusion about a trend"), not architectural. Grepping `REDLINES.md`, `AGENTS.md` and `docs/specs/*.md` for `deterministic|eyeball|aggregat` returns zero hits: the rule the review wants -- aggregation over history is deterministic code the model reads, never the model reading raw lists -- exists nowhere enforceable. `REDLINES.md`'s six rows cover thresholds, append-only history, loud sync failure, CTL warm-up, secrets and pinning; none of them this.

**The other side.** Adding a redline for one historical mistake is over-governance -- the decision log _is_ the mechanism, the error was caught, and `REDLINES.md` is explicitly merge-blocking machinery for a lesson. Not persuasive here, for three reasons. The error produced a confident recommendation to abandon the goal race, so the blast radius was the whole project. It is the single most likely failure mode of an LLM-fronted planner, and F11 and F17 rest on the same premise, so the rule is load-bearing rather than commemorative. And an append-only log is where lessons go to be not-read: a decision entry from six weeks ago will not stop the next agent. The cost is four lines.

**Justification.** Accept the substance, reject the action as a no-op. "Keep it as the standing example" changes nothing and leaves the lesson in the one artefact that is written to and never read back. `REDLINES.md`'s own escape hatch -- apply the rule, log the enforcement gap in `docs/ci-gates.md` -- is designed for exactly a rule that cannot yet be mechanically checked because the code it governs does not exist. One live instance of the hazard already sits in the repo: `src/data/recent-activities.json` is a 30-row committed snapshot, correctly labelled as to freshness but carrying no declaration of the window it completely covers, and that boundary had to be established by hand before the F7 ratios could be computed from it.

**Action.** Add one row to `REDLINES.md`: any trend, aggregate or ramp figure that informs a plan is computed by code from the full series and returned as a number with its window stated; the model never derives one by reading an activity list. Add the matching NOT IMPLEMENTED row to `docs/ci-gates.md`. Amend the `docs/decisions.md:393-396` consequences paragraph to point at the new redline so the entry and the rule are linked. When the aggregate helper lands, have it return the coverage window alongside the value, so a caller cannot silently compute a trailing-30-day figure from a snapshot that does not span thirty days.

<a id="f11"></a>

### F11. Deterministic planner as sole writer is right -- ACCEPT-MODIFIED

**Verification.** `docs/specs/04-mcp-surface.md:5-27` lists every v1 tool; all eleven are typed, parameterised writes (`daily_checkin`, `log_activity`, `replan(reason)`, `adjust_session(session_id, changes)`, `set_availability`). There is no free-form plan-write tool and none that accepts a session array, so the sole-writer property holds structurally today. `:30` carries the negotiation half in prose. `REDLINES.md`'s six rules were read in full; none says the planner is the sole writer. Errata: the finding's action cites "the propose/validate seam (F18)"; F18 is intervals.icu -- the seam is F28.

**The other side.** A rigid planner feels like a spreadsheet and defeats the stated wish that discussions with Claude dynamically shift training. It does not win. The constraint being defended is not rigidity of _content_ -- `replan(reason)` is free text and can move anything -- it is rigidity of _authorship_. Appendix A's SafeRun figure (79.1% versus 100% hard-constraint satisfaction) is about who evaluates the constraint, not who chooses the session. A planner that accepts arbitrary natural-language intent and validates deterministically is exactly as conversational as one that writes directly, and is the only version where a guardrail means anything.

**Justification.** Accept the endorsement -- verified true in the tool surface as written. Modified because a property that holds only because nobody has yet added a convenient tool is an accident, not a foundation, and the cheapest way to convert it is one row in a table that already exists. Not a design change; F28 owns the actual seam.

**Action.** Add one row to the `REDLINES.md` rule table, after the append-only row: "Session and week rows are written only by the deterministic planner. No MCP tool accepts a plan, a session list, or a free-form schedule from the model; every write is a typed, guardrail-validated intent." Not mechanically checkable yet, so it also needs a NOT IMPLEMENTED row in `docs/ci-gates.md`. No code change.

<a id="f12"></a>

### F12. Readiness over-trusts the watch -- ACCEPT

**Verification.** The live block in `config/training.ts` is `READINESS`: `subjectiveWeights {soreness .45, sleep .2, motivation .15, rpeYesterday .2}`, `objectiveWeights {hrvVsBaseline .4, restingHrVsBaseline .3, sleepScore .2, bodyBattery .1}`, `objectiveShareWhenAvailable 0.4`, `greenFloor 0.7`, `amberFloor 0.45`. Nothing outside config reads it -- grepping `src/` and `config/` returns only `config/training.ts` and its test, which assert the two weight sets sum to 1, that soreness dominates, and that `greenFloor > amberFloor`. Stronger evidence than the review had: `tools/garmin_probe/CATALOGUE.md:20-25` records `hrv_day` at 2 bytes, `hrv_range_7d` at 2 bytes, `sleep_daily_7d` at 2 bytes, and `:758-770` shows `sleep_day` with `sleepTimeSeconds`, `sleepStartTimestampLocal` and every sleep-stage field null. `tools/garmin_probe/garmin.py:299-320` confirms these were probed for yesterday and a seven-day range, so it is a week of absence, not a one-day miss.

**The other side.** The Fenix 8 is top-tier hardware, `docs/decisions.md:245-247` already declines to adopt Garmin Training Readiness as the score, and the 40% applies only when data exists and weights renormalise. The renormalisation clause is the argument that loses, and it loses on evidence the review did not have. With HRV (0.4) and sleepScore (0.2) empty for a full week, renormalisation does not protect the score -- it **concentrates** it: the survivors are `restingHrVsBaseline` and `bodyBattery`, renormalising to 0.75 and 0.25 of a 40% share. That puts 10% of the whole readiness verdict on Body Battery, a Firstbeat composite Appendix B classes [F] with no independent validation, and 30% on a single resting-HR reading. Declining Garmin's Training Readiness as the _score_ while sourcing 40% of our own score from the same unvalidated sensor stack is the same bet with an extra step.

**Justification.** The claim is right, the direction is right, and the code state makes the change free -- no consumer, one test file asserting internal consistency only. Under option A the readiness formula is Stage 9 and lands after 24 October, so this is a config-and-spec edit now, not engineering. Do it while it costs nothing rather than after something reads it.

**Action.** In `config/training.ts` `READINESS`: (1) `objectiveShareWhenAvailable` 0.4 -> 0.3. (2) Add `objectiveCanOnlyDowngrade: true` and specify the formula in the docstring so Stage 9 cannot misread it -- `score = min(subjective, 0.7*subjective + 0.3*objective)`, one expression, mathematically veto-only, cannot lift amber to green. (3) Add an `hrv` shape block `{rollingMeanDays: 7, baselineDays: 60, swcBandSd: 0.5}` and state that `hrvVsBaseline` reads the rolling mean against that band, never a raw daily RMSSD. (4) Record the probe evidence (`CATALOGUE.md:20-25`) that `hrvVsBaseline` and `sleepScore` currently have no data at all, and require Stage 9 to surface which objective terms were present rather than silently renormalising. Then update `docs/specs/02-load-engine.md:22-24` to match, add a `docs/decisions.md` entry citing Saw 2016 and Düking 2021, and add one test asserting `objectiveShareWhenAvailable <= 0.3` -- the existing tests pass unchanged through any share value.

<a id="f13"></a>

### F13. Late swims will corrupt the readiness inputs -- ACCEPT-MODIFIED

**Verification.** The premise is half wrong. `docs/specs/02-load-engine.md:13` reads "Swim: real training stress (learning stroke is fatiguing) but near-zero musculoskeletal/impact component", and `docs/plans/PLAN-2026-001-m1-core-loop.md:560` reads "Swim: full cardio credit, `surface ~ 0.05` musculoskeletal". `MULTIPLIERS.swimMusculoskeletal = 0.05` is the musculoskeletal multiplier only; there is no near-zero cardio term anywhere. On the HRV half: `AVAILABILITY.swimSessionsPerWeek` is 1, not five, so the exposure is one evening a week. Swim end time needs no new column -- `src/db/schema.ts:124` `startTimeLocal` plus `:131` `durationS` already yields it. Bedtime has no source: `src/db/schema.ts:204-219` `checkIns` carries `sleep` as a rating with no bedtime, and `CATALOGUE.md:764-767` shows `sleepStartTimestampLocal` null. And the inputs the rule would protect do not exist -- seven days of empty `hrv_day`/`hrv_range_7d`/`sleep_daily_7d`.

**The other side.** The mechanism is real and well-powered (Leota 2025, 14,689 people, 4 million nights) and early front-crawl is genuinely breathless, so the physiology is not in dispute; the fix is cheap, and a corrupted HRV reading misread as training fatigue could cost a session in the peak week. That fails on two checkable counts. Blast radius: the appendix was briefed against five swim evenings; at one a week the rule fires at most seven times in the whole block. And decisively, it cannot fire at all -- there is no overnight HRV and no sleep score in the account to corrupt. Building an exclusion rule for absent inputs is dead code, and F12's veto-only change already means a low objective reading can only downgrade, never manufacture a false green.

**Justification.** **Reject the cost half outright**: the repo already gives swim full cardio credit in both the spec and the plan, and the review misread a musculoskeletal multiplier as a total-cost one. Accept the HRV half as sound physiology but not as pre-race work -- it is contingent on an open question (`docs/specs/07-wiring-todo.md:22`, confirm whether the watch is worn asleep) and on Stage 9, deferred past 24 October. Record the rule so it is not rediscovered; do not implement it.

**Action.** Do **not** add a swim-end-time column. Add one paragraph to `docs/specs/02-load-engine.md` under Readiness: a swim ending after 20:30 local flags the following morning's HRV and sleep-score terms as suspect; they drop from the objective block for that day and the weights renormalise -- wall-clock cutoff rather than bedtime-relative, because no bedtime source exists. Add a `docs/decisions.md` entry citing Leota 2025, recording the one-session-a-week blast radius and that the rule is inert until the watch is worn asleep. Correct the review's ledger note that the spec undercounts swim cardio -- it does not.

<a id="f14"></a>

### F14. TSB must never gate a session on its own -- ACCEPT

**Verification.** The supporting claim checks out: `docs/plans/PLAN-2026-001-m1-core-loop.md:594` reads "Do not gate on an acute:chronic ratio crossing a threshold. ACWR has been statistically dismantled -- Lolli 2019 ... Impellizzeri 2020", and `config/training.ts` `CHECK_IN_GATES` criterion `acute-chronic-trend` carries the same refusal in the same words. But the finding is understated, not redundant: PLAN line 151, open question 4, carries a standing recommendation for ratification saying the opposite -- "musculoskeletal TSB gates _quality_ sessions only; cardio TSB drives the overall green/amber/red." That is TSB gating a session on its own, awaiting signature. Second live gap: `docs/specs/02-load-engine.md:17` says "Ramp rate = week-over-week CTL change; the guardrail input (see 03)", while `docs/specs/03-planner.md:23` and `GUARDRAILS.rampCapPct` both define the ramp cap on weekly run km. Two specs disagree about what the ramp guardrail measures, and one routes a decision through CTL. Nothing in `src/` reads LOAD or TSB, so both are documents-only fixes.

**The other side.** The repo already refuses ratio-gating and TSB is a difference rather than a ratio, so Impellizzeri's mathematical-coupling critique does not transfer -- making this a request for a line the repo already lives by. It loses on the open question: PLAN line 151 is not idle prose, it is queued for ratification and blocks Stage 9, so if it is signed the plan ships TSB-gated quality sessions and the `CHECK_IN_GATES` paragraph becomes decoration. The coupling critique is also beside the point; the criticism that transfers is Appendix B Q1's -- the 7/42 constants were chosen as nominal values, are non-identifiable (Hellard 2006) and shift with the load metric (Vermeire 2022), so the number has no calibrated meaning to threshold against. That applies to a difference exactly as to a ratio.

**Justification.** Accept. The principle is honoured in two places and contradicted in a third that is queued for approval -- precisely the shape of finding worth acting on, because the contradiction is cheap now and expensive after ratification. The `02`/`03` disagreement about the ramp axis is a separate verified defect the finding surfaces incidentally and should be fixed in the same edit.

**Action.** Three document edits, no code. (1) `docs/plans/PLAN-2026-001-m1-core-loop.md:151` -- withdraw open question 4's recommendation and replace it with: musculoskeletal load is a trend display; quality is gated by the soreness gate (`READINESS.sorenessBlocksQuality`) and the readiness verdict, never by a TSB threshold. (2) `docs/specs/02-load-engine.md`, after line 17 -- "ATL, CTL and TSB are trend displays. No session is gated on a TSB or CTL value. The 7/42 constants are conventions, not fitted parameters (Hellard 2006, Vermeire 2022). Decisions come from the single-session cap, the weekly ramp cap, the soreness gate and readiness." (3) Fix `docs/specs/02-load-engine.md:17` -- the ramp guardrail input is weekly run km, matching `03-planner.md:23`; CTL change is reported, not enforced. Add a `docs/ci-gates.md` NOT IMPLEMENTED row for "no decision reads TSB" so the rule is tracked rather than trusted.

<a id="f15"></a>

### F15. Taper, quality count, multipliers, pace ratio -- ACCEPT-MODIFIED

**Verification.** _Taper:_ `docs/specs/03-planner.md:26` already permits intensity in taper, and `GUARDRAILS.protectedTaperWeeks: 2` is worded on volume, not intensity -- the "no intensity" reading the finding warns against is not in the repo. Bosquet compliance from the committed `BLOCK_WEEKS`: peak 100 -> 80 -> 60 (-40% on peak) -> week 7 `targetKm` null. Two gaps: week 7 has no number, and the phase labels put "taper" on weeks 5 and 6 while `protectedTaperWeeks: 2` selects 6 and 7, so "taper week 1" in `03-planner.md:26` no longer identifies the week the LDNX 10K falls in. _Quality:_ `maxQualitySessionsPerWeekBuild: 1`, `minDaysBetweenQualitySessions: 2`; grepping for the former in `src/` and the test file returns nothing -- no consumer, not even a test. The block has **three** build weeks, not four, so Filipas's "final four build weeks" has no referent here. _Multipliers:_ `MULTIPLIERS` is already headed "PROVISIONAL, all of them" and has no production consumer. _Pace:_ 12940/5892 = 2.1962, confirming the arithmetic; `planningBandSeconds` is consistent with the 2026-09-07 goal-as-range decision.

**The other side, argued both ways as the review asks.** _For_ two quality sessions: Filipas 2022 is a 60-runner RCT and the strongest citation in Appendix B Q5; a 3:50-4:00 runner's limiter is marathon-pace durability; one session a week yields about three structured sessions in the whole block; and the reviewer's own alternative -- marathon-pace segments inside the long run -- is a second quality stimulus wearing a different label, so refusing to count it is bookkeeping rather than physiology. _Against_, and this wins: Filipas measured a ~1.5% 5 km gain in well-trained runners on stable volume over 16 weeks, and this block is three build weeks inside a 35->100 km ramp, roughly 75% above anything in `MEASURED_BASE`, in a new shoe. The outcome that matters is marathon time, and Doherty 2020's meta-regression over 127 cohorts associates faster marathons with weekly km, runs per week, longest run and runs over 32 km -- volume parameters, not quality-session count. A second interval session and the easy kilometres the evidence actually links to marathon outcome draw on the same recovery budget. Decisively, the block is not short of intensity: `RACES` already puts a maximal half on 12 September, Lincoln at marathon pace on 4 October and a raced 10K on 11 October. Lincoln alone is a 21.1 km marathon-pace block.

**Justification.** Split. **Reject** the second quality session for this block. But the real defect F15 exposes is not the number 1 -- it is that 1 is ambiguous and unenforced: nothing says whether a race counts against the budget, so week 4 could legally hold Lincoln-at-marathon-pace plus a separate interval session on top of a 100 km peak, which is the exact stacking the injury argument exists to prevent. Fixing that is worth more than the 1-versus-2 debate. **Accept** the taper point as almost entirely already satisfied, with two numbering gaps. **Reject** the descent-term refinement as pre-race work -- `MULTIPLIERS` has no consumer and Stage 8 is deferred, so it tunes a number nothing reads. The unvalidated-priors labelling and the pace sub-point are already done.

**Action.** (1) Keep `maxQualitySessionsPerWeekBuild` at 1 and record the decision in `docs/decisions.md` with both arguments -- Filipas 2022 for the rejected side, Doherty 2020 plus the three-race intensity load for the accepted side. (2) Close the ambiguity: add `racesCountAsQualitySessions: true` to `GUARDRAILS` and state in the docstring that marathon-pace segments inside a long run do **not** count separately. Add a test -- the threshold currently has none -- asserting no `BLOCK_WEEKS` week holds both a `LIVE_RACE_DATES` date and a separate `kind: 'quality'` day. (3) Set week 7 `targetKm` (~30-35 km) so the two-week taper is a checkable number rather than a null. (4) Reword `docs/specs/03-planner.md:26` to name the LDNX 10K by date (11 October) rather than by "taper week 1", and state explicitly that the taper cuts volume while holding frequency and intensity (Bosquet 2007). (5) No change to `MULTIPLIERS.descentPer100m` before the race; note the grade- and pace-dependent form in the plan's Stage 8 section as the calibration to adopt when the component is built.

<a id="f16"></a>

### F16. The two-component load model is correctly deferred -- ACCEPT

**Verification.** The deferral is real and reasoned: `docs/plans/PLAN-2026-001-m1-core-loop.md:124-139` heads it "Deferred hardest", names it "the weakest bet in the project", records that no published validation exists, that the coefficients are calibration knobs not findings, that calibration needs prediction-versus-outcome pairs which cannot exist yet, and sets an evidence-based revisit trigger (~3 weeks of Stage 9 running alongside Garmin's own series). PLAN `:51` and `:181` repeat the honesty in the assumptions and pre-mortem tables. Stage 8 is `:549-575`, Stage 9 `:582-604` and blocked on open questions 4 and 5 -- both after the MVP boundary at `:118-120`, and consistent with the 2026-09-07 option-A decision. One live inconsistency the finding does not mention: the repo describes the split in the **present tense** as if it exists -- `config/training.ts` `LOAD` docstring reads "Stress is tracked as TWO components", `docs/specs/02-load-engine.md:11` says "Track cardio stress and musculoskeletal stress as two components", and `docs/specs/07-wiring-todo.md:35` says the opposite ("start: total-load only").

**The other side.** The two-component split is what the whole design exists for -- it is the lived 2026-08-09 lesson (a 33 km/600 m trail run scored like a flat 30 km road run and wrecked him for a week), and this block stacks the same three novel stressors again: a new daily trainer, a 100 km peak, a 75%-above-history ramp. Deferring the one model built to catch that defers it exactly when it would earn its keep. It does not win, for a reason the code makes concrete: the mitigations that actually carry this block are structural, not computational -- `minRunDaysAtHighVolume` spreading 100 km over seven days, the week-2 check-in gate, `SHOES.trailAdaptationCapKm`, and the shoe-novelty prescription -- and all of them work without a musculoskeletal score. A model whose coefficients are admitted guesses would not have prevented 2026-08-09 either; a distance cap on first trail exposure would have.

**Justification.** Accept, no dispute. The deferral is well-reasoned, has a named revisit trigger, and matches the ratified scope. The one thing to fix is that the deferral is invisible in the present-tense prose, which is the kind of drift that has someone building against it in six weeks.

**Action.** No engineering. Change the mood of the claim in two places -- the `config/training.ts` `LOAD` docstring and `docs/specs/02-load-engine.md:11`: "Stress is **designed** as two components ... The first release scores total load only; the musculoskeletal component is Stage 8, deferred past 2026-10-24 (PLAN:124-139)." That also resolves the standing contradiction with `docs/specs/07-wiring-todo.md:35`. Leave `MULTIPLIERS` in place, marked PROVISIONAL and unused, so the calibration knobs survive for Stage 8. Keep open question 4 open -- F14's action withdraws its TSB-gating recommendation, and the remaining half cannot be answered until the component exists.

<a id="f17"></a>

### F17. Strava API use for an AI-fronted system is prohibited -- ACCEPT

**Verification.** Fetched the primary source `https://www.strava.com/legal/api_policy` directly, not the appendix. Confirmed verbatim, effective 2026-06-01: §5.3 "You may not use the Strava API Materials or Strava Data, directly or indirectly, in connection with the development, training, evaluation, or operation of any AI Application", extended to "any data derived from, aggregated from, anonymized from, or generated using Strava Data, in any form" and to "grounding, evaluation, benchmarking, embedding generation, retrieval-augmented generation". The carve-out is equally verbatim: "This prohibition does not extend to use of the Strava MCP" / "Subscribers to Strava may access the Strava MCP in connection with their personal use of their own Strava data". The policy does **not** define "AI Application". Second leg verified independently at `https://developers.strava.com/docs/reference/`: the Routes resource exposes only `GET /routes/{id}`, `GET /routes?athlete_id=`, `export_gpx`, `export_tcx` -- no POST/PUT/DELETE. So both stated justifications in `docs/STRAVA_SETUP.md:50` ("a fallback activity feed") and `:52` ("the publishing target when routr generates a route later") fail: the first is grounding under §5.3, the second is not a capability the API has. Carve-out path confirmed live on this account -- the Strava MCP eligibility check returns full access. Inventory: `tools/strava_probe/strava.py` (147 lines), `test_strava.py` (27), `docs/STRAVA_SETUP.md`, `tools/strava_probe/out/token.json` (gitignored, not tracked). No file under `src/` or `scripts/` references Strava. Coupling is zero.

**The other side.** Single-user personal display is arguably not an "AI Application", and no enforcement against individuals is documented. That fails on its own terms twice. It protects a use rocket does not have: the probe's stated purpose is a feed for a coach whose reasoning is an LLM, which is grounding, named explicitly in §5.3's list. And the undefined term cuts against the counter, not for it -- with no definition, the safe reading is the broad one, and the drafters bothered to write an explicit carve-out for exactly the personal-own-data case, which is strong evidence they did not consider single-user use already outside §5.3. The enforcement-risk argument is irrelevant because the cost of compliance is zero: 174 lines with no dependents, an MCP already authorised on the account returning richer data (per-second streams, zones), and Garmin primary anyway. Keeping it buys nothing and risks key revocation mid-block.

**Justification.** The primary source says what the review says it says, and the code's own justification for existing is falsified on both halves -- one prohibited, one impossible. Not a close call about interpretation: dead code carrying a licence liability. Deleting it is also the laziest outcome -- it removes 174 lines, a setup doc, an OAuth flow to babysit and a whole port table, and replaces them with a connector already authorised on the account.

**Action.** Delete `tools/strava_probe/` and `docs/STRAVA_SETUP.md`. Revoke the Strava API application at `strava.com/settings/api` and remove the `pass` entries `strava/client-id` and `strava/client-secret`. Rewrite `docs/specs/05-integrations.md:54` to say Claude reads Strava through the official Strava MCP when it needs streams, that rocket holds no Strava API credentials, and that routes cannot be published to Strava by API at all (routr delivers GPX; a Strava route is created by hand in their web UI, which is how DoHardThings' `stravaRouteUrl` fields get populated). Amend `tasks/integration-harness.md` to record the Strava half as withdrawn on ToS grounds. Add a `docs/decisions.md` entry citing §5.3 and the MCP carve-out.

<a id="f18"></a>

### F18. intervals.icu is the sanctioned Garmin path -- ACCEPT-MODIFIED

**Verification.** Luis's 2026-09-07 decision already adopts the recommendation, so this was assessed on the merits against probe output that did not exist when the review was written. `tools/garmin_probe/out/` holds 41 real payloads. Decisive in favour of the direct library: the two capabilities named as its only unique advantage are both present and populated -- `training_status.json` carries `acuteTrainingLoadDTO = {acwrPercent: 42, acwrStatus: OPTIMAL, dailyTrainingLoadAcute: 296, dailyTrainingLoadChronic: 287, dailyAcuteChronicWorkloadRatio: 1.0}`, and `training_readiness.json[0]` carries the full sub-factor breakdown (score 63, `recoveryTimeFactorPercent` 60, `acwrFactorPercent` 90, `hrvFactorPercent` 0, plus `validSleep: false` and `hrvWeeklyAverage` 511). Equally decisive against: `hrv_day.json` and `hrv_range_7d.json` are literally `{}`, `sleep_daily_7d.json` is `[]`, `max_metrics_vo2.json` is `[]`. The library works (`garmin_guard.py` implements a two-bucket sqlite rate guard -- LOGIN 2/day with a 72h breaker, DATA 20/min with a 30min breaker, reserve-before-call, fail-closed) but the wellness inputs are thin because the watch is not worn asleep. On the bridge side, field coverage could **not** be confirmed from a primary source: intervals.icu's features page names "Weight, sleep, HRV, readiness, glucose... auto-sync from Garmin" but does not name resting HR, Body Battery, or Garmin's Training Readiness.

**The other side.** The direct library is the only route to the readiness sub-factors and the acute/chronic pair, and the probe is built and working -- now stronger than the review knew, because the payloads prove both land with real values. It still does not win, for a reason the review only half-states: the decisive variable is not which source is richer but which runs unattended in production. `python-garminconnect` cannot run on Vercel (no durable token disk), its 429 is per-account and costs 48-72h with no recovery, and its login path regex-parses HTML. intervals.icu costs a daily HTTPS GET with a personal API key. The richness argument therefore collapses into an argument about a nightly job, and a nightly job that can lock the account for three days during a four-week build is not a nightly job. The honest resolution is not either/or -- the probe stays as a manually-run deep pull, which is the ratified decision. The "accommodate all available data" instruction recorded at `tasks/integration-harness.md:15` is satisfied by storing the bridge's raw payload plus the probe's catalogued fields, not by making the fragile source primary.

**Justification.** The decision is right and now has better evidence behind it than the review had. The modification is that the retained probe is load-bearing rather than a courtesy: the plan already commits at PLAN `:594` to warm-starting CTL from an external chronic value rather than a filling-window mean, and the only observed source of that value in this repo is the probe's `dailyTrainingLoadChronic = 287`. That number exists today and is worth capturing now, before any bridge decision, because it is the single input that stops the load model reporting false-red through the whole build.

**Action.** (1) Run the G1 probe (PLAN `:271`) before any wellness schema is designed, and record which of hrv, restingHR, sleepScore, bodyBattery and trainingReadiness actually arrive -- the vendor page does not answer it. (2) Capture `dailyTrainingLoadChronic = 287` and `dailyTrainingLoadAcute = 296` as the CTL/ATL seed now, from `tools/garmin_probe/out/training_status.json`, independently of the bridge decision. (3) Amend `tasks/integration-harness.md:15` ("Alternative rejected: the intervals.icu bridge") -- it now records a superseded instruction. (4) Surface to Luis as an operational fact rather than a question: `validSleep` is false and `hrv_day` is empty, so overnight metrics are absent from **both** candidate sources. Wearing the watch asleep is worth more to the readiness model than either integration.

<a id="f19"></a>

### F19. The plan is never on the wrist -- ACCEPT-MODIFIED

**Verification.** The gap is real. Grepping `docs/specs/` and `docs/plans/` for `push_session|push_week|workout push|structured workout` returns nothing. Every "watch" hit in `docs/specs/05-integrations.md:24` and `docs/specs/07-wiring-todo.md:17,22` treats the Fenix 8 as a data **source**, never a destination; `05-integrations.md` has no outbound section at all. The mechanism proposed is real: intervals.icu exposes `POST /api/v1/athlete/{id}/events` for planned workouts, and `python-garminconnect` has `upload_running_workout`/`schedule_workout` if the direct branch is ever used.

**The other side.** The finding's implicit argument is "every credible product does it", an appeal to product norms rather than to this athlete's need, landing in a fortnight whose ratified scope is a thin working coach. Weighed properly the norm argument mostly loses: this block is four build weeks of easy volume with one quality session and one long run per week, and a Fenix 8 user runs those to pace or feel without a compiled workout loaded. What earns its place is much smaller -- knowing at 06:00, on the wrist, what today's session is. That is a calendar event, not a structured workout.

**Justification.** The observation is correct and the omission genuine, but the prescribed size is wrong for the clock. **Reject the framing** (a `push_session_to_watch` cron stage plus a `rocket_push_week` tool, implying a workout compiler); accept the core -- the plan has no outbound leg at all, and once the bridge exists the calendar leg is one POST reusing the API key the bridge already needs.

**Action.** Add a short "Outbound: the plan on the wrist" subsection to `docs/specs/05-integrations.md` specifying one direction only -- the planner's session for the day is written as an intervals.icu event, which Garmin Connect surfaces on the watch. Implement it as a single call appended to whatever daily job the bridge already runs, not as its own stage or MCP tool. Explicitly out of scope until after 2026-10-24: structured workout steps, pace/HR targets, and any Garmin-direct workout upload.

<a id="f20"></a>

### F20. Google Calendar is already a connector; ICS ingest is unnecessary -- REJECT

**Verification.** Both halves check out factually and neither produces a change worth making. The connector exists in this very session (`list_events`, `search_events`, `suggest_time` are live tools). But `docs/specs/05-integrations.md:52` already reads: "v1: AvailabilityRule pattern ... + manual overrides via `set_availability`. ICS feed integration is a later nice-to-have; don't block on it." That sentence already carries the correct disposition -- deferred, explicitly non-blocking, zero cost to the current build. It is one clause in one line and it gates nothing. Meanwhile the substantive half is wrong for rocket's architecture: F11 makes the deterministic planner the sole writer, and the planner runs as a Vercel cron with no Claude session and therefore no access to a claude.ai connector. A conversational session can read the calendar; the thing that actually needs availability cannot.

**The other side.** Specs accumulate dead deferrals and each one is a future agent's wasted hour, so striking the clause is free hygiene. Granted -- but that argues for deleting eleven words, not for a finding, and the finding does not stop there. It asserts "Work availability is a solved read in chat", and acting on that would be actively harmful: it would justify not persisting availability, leaving the cron planner with nothing to place sessions against. The spec's existing design is correct precisely because it does not depend on a connector being in the loop, and the review's own F27 (everything that affects the plan is a tool write) contradicts it.

**Justification.** A finding whose accepted half changes one already-hedged clause and whose asserted half is wrong about where the planner runs is not worth carrying. Rejecting it also protects the correct design from a later agent reading the review and concluding availability need not be stored.

**Action.** None. Optionally, when `05-integrations.md` is next edited for another reason, drop the trailing "ICS feed integration is a later nice-to-have; don't block on it." from line 52 -- but do not touch the `AvailabilityRule` design, and do not record anywhere that Claude reads the work calendar in place of stored availability.

<a id="f21"></a>

### F21. Garmin bulk export for backfill remains right -- ALREADY-DONE

**Verification.** The plan already says exactly this in the same terms. Stage 3b at `docs/plans/PLAN-2026-001-m1-core-loop.md:380`: "Depends on: Stage 3 (store seam). Independent of Decision gate G1 -- the bulk export works under either branch." Line 272 and the action table at `:670` both instruct: "Request the Garmin bulk export (Connect web -> Account Settings -> Export Your Data) ... 24-48h asynchronous, free, zero ToS risk, and the only sanctioned complete-history source. Requesting it now removes it from the critical path later. Do this first." Line 666 confirms it gates Stage 3b. The review's own action is "ACCEPT the plan's Stage 3b source", so the finding endorses rather than changes the repo. What is **not** done is the request itself: grepping `docs/decisions.md` and `tasks/*.md` for "bulk export" returns no entry recording that it was submitted, and the Stage 0 checklist at `:738` still has S0 unticked.

**The other side.** The finding adds information the plan lacks -- that under the bridge, historical import may cover activities but not wellness, so the export matters more. Fair, but it strengthens an instruction the plan already gives with maximum emphasis, so it changes no decision. Its real contribution is a reminder, and reminders belong on a checklist rather than in a ledger.

**Justification.** Nothing in the repo needs to change: source, independence from G1, urgency and ordering are all already written down correctly. The only live item is the human action, which has a 24-48 hour turnaround and 47 days of clock behind it -- worth escalating precisely because it is asynchronous and costs two minutes.

**Action.** No spec or plan edit. Escalate the human step to Luis today: Garmin Connect web -> Account Settings -> Export Your Data. Record the request date in `docs/decisions.md` so Stage 3b can tell whether it is waiting or the mail was missed. Add one line to Stage 3b noting that if the bridge is primary, its historical import is expected to cover activities but not wellness, so the export is the only route to 42+ days of wellness history.

<a id="f22"></a>

### F22. MCP stack: do not chase SDK v2 -- ACCEPT-MODIFIED

**Verification.** Checked npm directly, because the finding and the plan flatly contradict each other. Both are partly right. The plan is right that v2 exists: `@modelcontextprotocol/server@2.0.0` is real (created 2026-04-01, last modified 2026-07-28), and `@modelcontextprotocol/sdk` is at 1.30.0 -- but 1.30.0 was published 2026-07-27, so "frozen" (PLAN `:36`) overstates it; v1 is maintained, not abandoned. The plan is **wrong** that `mcp-handler` should be dropped (`:37`, "now a thin re-wrapper -- drop it"): `mcp-handler` is at 2.1.1, published 2026-08-13, `peerDependencies {"@modelcontextprotocol/server": "^2.0.0", "next": ">=13.0.0"}`, and its README states it serves the 2026-07-28 MCP specification natively with fallback for 2025-era clients. Vercel's package tracked v2 rather than being displaced by it. DoHardThings' stack verified from installed source rather than package.json ranges: `mcp-handler` 1.1.0, `@modelcontextprotocol/sdk` 1.29.0, mounted at `app/api/mcp/[transport]/route.ts:1-40` via `createMcpHandler` with `basePath /api/mcp`, running in production against the real claude.ai connector on iOS. One caution the review does not mention: `mcp-handler@1.1.0`'s peer pins the SDK at exactly 1.26.0, so DHT's 1.29.0 is an unmet peer that happens to work -- pin the versions DHT actually has installed, not the peer range.

**The other side.** "Both can be true; the question is which costs less" is the right frame, and cost is where the plan loses on a concrete point the review missed. PLAN `:426` prescribes DHT's v1 convention ("`inputSchema` as a plain object of zod validators, not a wrapping `z.object()`") for Stage 4, which the plan builds on SDK v2. `mcp-handler`'s v2 migration notes say the opposite for the v2 API: input schemas now require a complete Standard Schema (e.g. `z.object(...)`) rather than raw zod shapes, along with removed variadic registration and a changed handler signature. If that carries through from `server` v2 -- likely, since `mcp-handler` delegates to it, though unverified by running v2 -- the plan simultaneously mandates v2 and the v1 calling convention, so DHT's tool code does not port verbatim under the plan's own choice. That is the unpriced cost: not the SDK, the tool layer. The plan hedges sensibly elsewhere (`:92` names "v1 + mcp-handler is the retreat", `:40` already knows claude.ai negotiates 2025-11-25), so it is not naive -- but a documented retreat plus a Med-High Stage 0 spike is worse value than starting on the thing that already runs.

**Justification.** Reuse beats research when the clock is two weeks and a working instance of the exact stack exists on this account. The DHT stack is proven end to end against the real connector including iOS, and its tool layer, error mapping and OAuth shim port as-is only if the SDK major matches. Adopting v2 buys a protocol revision no client on this account negotiates, and costs a rewrite of the one part that was going to be copied for free. The plan's factual claim about v2's existence stands; its conclusion does not.

**Action.** Amend PLAN `:36-37` and `:92`: v1 is current and maintained (1.30.0, 2026-07-27), v2 shipped as the renamed `@modelcontextprotocol/server`, and `mcp-handler` was not superseded -- it is at 2.1.1 on v2. Build Stage 4 on `mcp-handler@1.1.0` + `@modelcontextprotocol/sdk@1.29.0`, pinned exact, copying DoHardThings' `app/api/mcp/[transport]/route.ts` and its `ok()`/`fail()`/`reason()` layer. Delete the Stage 0 SDK v2 spike (`:268`) and the zod-3 probe (`:270`) -- DHT proves the stack in production on this account, and the `npm ls zod` CI gate at `:429` covers the zod risk at zero cost. Keep the retreat note pointing the other way: revisit `mcp-handler@2` after 2026-10-24.

<a id="f23"></a>

### F23. Auth for a single user: do not build an OAuth server first -- ACCEPT-MODIFIED

**Verification.** The finding's action step is already answered, twice, empirically, on this account -- so the step is rejected and the conclusion accepted. Anthropic's own docs list `static_headers` as Beta and describe it as a fixed credential entered by an **organization administrator** as a request header, shared by the organization rather than pasted per user -- an org-admin surface, not a Pro individual one. Empirically: `routr/docs/MCP_CONNECTOR_GUIDE.md:14-24` states "claude.ai's custom connectors ignore a static token and run the full OAuth flow -- established the hard way on the DoHardThings project, not guessed ... There is no field for it in the connector dialog"; `routr/docs/plans/PLAN-2026-004:125` carries it as a risk row ("static-header beta not enabled for this account"); DHT's route file comment records the same split. So the five-minute check returns NO and is not worth doing. The docs **do** confirm `none` (authless) is supported, with Anthropic egress fixed at `160.79.104.0/21`. Scale: rocket's Stage 5 is six route files plus `jwt.ts` plus tests, porting DHT's `lib/mcp-oauth.ts` (227 lines) -- "~600 lines plus the highest-scrutiny security review" is fair. Ordering defect confirmed: Stage 5 sits before Stage 6 while Stage 4 at `:412` is already flagged "First end-to-end usable milestone".

**The other side.** If `static_headers` is unavailable, OAuth is unavoidable for the phone, and since the shim is a near-verbatim port its marginal cost is lower than it looks. The first clause is now established fact rather than a risk. The second is true and is exactly why the conclusion survives anyway: a near-verbatim port is cheap whenever it is done, which argues for doing it **last** -- nothing about it gets harder by waiting, and it is the only stage whose output is zero coaching. The real tension the finding under-states is the consequence of deferring: with OAuth past 2026-10-24, the coach is reachable from Claude Code and desktop but **not from the phone** during the entire block, which is where a 06:00 check-in actually happens. Authless plus an Anthropic-egress allowlist closes that for roughly ten lines instead of six hundred; the residual risk is that anyone who learns the URL reads Luis's training data, mitigated but not eliminated by an unguessable path and the append-only guards in `src/db/migrations/0001_append_only_guards.sql`.

**Justification.** The conclusion is right and the evidence is stronger than the review had. What needs modifying is the action: skip the check, fix the ordering, and name the phone-access consequence rather than leaving it implicit, because "defer OAuth" and "a coach you talk to from bed" are in direct tension for the next seven weeks.

**Action.** (1) Drop the "check the connector dialog" step -- record in `docs/decisions.md` that `static_headers` is org-admin-only and beta and that claude.ai discards static tokens, so OAuth or authless are the only phone paths. (2) Reorder PLAN-2026-001 so Stage 5 sits after the planner and the check-in loop, and Stage 4's bearer-gated Claude Code coach is followed by coaching rather than auth plumbing. (3) For phone access inside the two-week window, ship the MCP route with auth type `none` at an unguessable path, gated by an allowlist on Anthropic's egress range `160.79.104.0/21` -- about ten lines, and the docs confirm authless connectors are supported. **One line for Luis to veto** (section 4). (4) When Stage 5 does land, port routr's shim or DHT's `lib/mcp-oauth.ts` with the owner-email allowlist and typ-claim fixes the plan already specifies -- do not write it fresh.

<a id="f24"></a>

### F24. PWA: manifest plus a hand-written service worker, no Serwist -- ALREADY-DONE

**Verification.** The repo already implements exactly this, and the finding says so ("ACCEPT the task's scope"). `src/app/manifest.ts` is a Next `MetadataRoute.Manifest` with `display: standalone` and three PNG icons plus `src/app/apple-icon.png`; its own comment reads "iOS 26 removed installability requirements entirely ... No service worker is needed." `tasks/homescreen-app.md` records both rejections the finding endorses -- DHT's `components/pwa/` install journey ("roughly 26 KB of mock-Safari-chrome walkthrough that drags in motion, lucide-react, sonner and @base-ui") and the service worker ("not required for home-screen install, and offline is meaningless for a page whose value is freshness"). `package.json` has `next` 16.3.1 and no `serwist`/`next-pwa`; DoHardThings by contrast has `@serwist/next` with `swSrc: "app/sw.ts"`. The port boundary is precisely right and worth recording: DHT's `public/sw.js` is a single minified serwist build artefact and does **not** port, but `lib/sw-push.ts` (52 lines, pure helpers, unit-testable by design) and `lib/push-*.ts` (461 lines across client, payloads, reminders, send, store) are framework-free and do; `app/sw.ts` is 137 lines of glue that would be rewritten as a plain `public/sw.js`.

**The other side.** The finding's aside that the task was "correct for now, wrong once push lands" reads as a latent defect and is not one: a service worker added before there is anything to push would be untested scaffolding, and adding it at push time costs the same twenty lines either way. There is no compounding cost to waiting, so the task's call is right at both points in time. The one useful contribution is the port map -- a note for a future session, not a change today.

**Justification.** Nothing to accept or reject: the code already matches the recommendation, and the reasoning is written down in the task file in more detail than the finding carries. The residual value is the port boundary, worth capturing while it has been verified rather than rediscovered later.

**Action.** No change. Append one line to `tasks/homescreen-app.md` recording the verified port boundary for whenever push lands: port `lib/sw-push.ts` and `lib/push-*.ts` from DoHardThings; do **not** port `public/sw.js` (serwist build output) or `app/sw.ts` (serwist glue) -- write a plain `public/sw.js` instead, since rocket is Next 16 on Turbopack and serwist targets webpack.

<a id="f25"></a>

### F25. Rocket stays a separate repo -- ACCEPT-MODIFIED

**Verification.** Rocket already **is** a separate repo. DoHardThings has no relational DB (grepping its `package.json` for `drizzle|prisma|postgres` returns nothing). The lift-table line counts spot-check accurate to rounding: `lib/mcp-oauth.ts` 227 + `lib/mcp-auth.ts` 81 + the register and token routes 36+77 = 421 of the claimed ~600 (the authorize route is not counted separately, so plausible); `app/api/mcp/[transport]/route.ts` is 63 against "60"; `lib/ics.ts` + `lib/ics-feed.ts` = 232 exactly as claimed; the six utilities 358 against "~400"; the push stack 650 against "~500" -- the review **under**-counted. The Strava store triple exists.

**The other side.** "One app, one connector, one login", whose strongest ground is that rocket's Race entity is just a projection of DoHardThings' race calendar. That dies on evidence discovered after the review: the note above `RACES` in `config/training.ts` records that DoHardThings stores race domain fields in Google Calendar `extendedProperties`, and the Google Calendar connector does not return `extendedProperties`. DoHardThings' own race data is therefore not queryable by a third party through the store it lives in -- the `RACES` list cannot currently be derived and is ratified by hand. Merging would inherit an unqueryable store for precisely the one entity the counter-argument says is shared.

**Justification.** The verdict is right and already the status quo, so only the lift table is actionable -- and the 2026-09-07 scope decision deletes its single largest row (~600 lines of OAuth shim). Under option A the lift order inverts: the utilities, the ICS pair and the `mcp-handler` wiring (63 lines, verbatim) are the near-term reuse; the push stack lands last per the repo's own 2026-09-06 service-worker decision; the OAuth shim is post-race. Modified rather than accepted as written, because following the table's implicit ordering would spend the first days of a two-week window on the row that was just deferred.

**Action.** Re-rank the F25 lift table in the plan for option A: (1) `mcp-handler` wiring, (2) utilities, (3) ingest ordering pattern, (4) ICS pair, (5) push stack, (6) OAuth shim -- post 24 October. Do not open the merge question again; record the `extendedProperties` finding as the closing evidence.

<a id="f26"></a>

### F26. Capability packs -- ACCEPT-MODIFIED

**Verification.** routr's contract confirmed read-only in `~/dev/routr/backend/app/mcp_server.py`: `generate_routes` at `:219`, `off_road_pct` at `:143`, `gpx_url` at `:155`, `surface="road_only"` a hard filter at `:247` and `"trail_only"` a preference at `:252`. Rocket side: `docs/specs/04-mcp-surface.md:7-27` names all eleven tools **without** a prefix (`get_status`, `replan`, `adjust_session`, `sync_now`); `propose_route` exists at `:25` marked "(post-routr wiring)"; no `import_race` tool exists anywhere. No `docs/specs/08-capability-packs.md` exists.

**The other side.** The review's own counter (a pack can be a seam plus a tool) agrees with the finding rather than opposing it. The real counter is: why proxy routr at all when Claude can call routr directly and read the numbers back into rocket via a normal write tool? That loses on a verified detail -- routr is a stateless generator whose candidates expire in ~30 minutes, so a route Claude fetched during a conversation is gone before the run happens. Persistence, not context bloat, forces the seam. The bloat argument alone is weak at four servers and the review concedes as much.

**Justification.** Both seams are correct in principle, and routr and DoHardThings seams are deferred past 24 October, so `08-capability-packs.md` is post-race work. But one sub-item has a hard deadline the deferral must not swallow: the `rocket_` prefix. Tool names are fixed into the connector when it is added on claude.ai, into every promptfoo assertion, and into muscle memory; renaming after the connector is live means re-adding it. Renaming now costs a find-replace in a spec file with zero implementation behind it. That is the one piece that cannot wait.

**Action.** Rename all eleven tools in `docs/specs/04-mcp-surface.md` to the `rocket_` prefix now. Defer `docs/specs/08-capability-packs.md` and both seams past 24 October, with a register row naming the 30-minute candidate expiry as the reason `propose_route` must persist rather than browse.

<a id="f27"></a>

### F27. State and memory: everything that affects the plan is a tool write -- ACCEPT-MODIFIED

**Verification.** `src/db/schema.ts` has exactly five `pgTable` calls -- `weeks:43`, `sessions:62`, `races:90`, `activities:111`, `check_ins:203`. No `notes`. But the review picked the one example already modelled: `docs/specs/01-domain-model.md:30` assigns "in Leeds Thursday" and "half day Friday" to `AvailabilityRule` one-off overrides, and `:28` gives `CheckIn` a free-text note. `docs/plans/PLAN-2026-001-m1-core-loop.md:294` defers `availability_rules` to Stage 6.

**The other side.** The sharpest counter is not the review's memory argument but this: `check_ins` already carries a free-text note and `set_availability` already carries overrides, so `add_note` is a third door into two existing rooms. That fails on mutability, and the reason is in the repo. `src/db/migrations/0001_append_only_guards.sql` makes `check_ins` append-only under an `ENABLE ALWAYS` trigger, keyed to a day that has already happened. A constraint about next Thursday cannot live in an immutable row about this morning, and it must be revocable when Thursday moves. Different mutability class, different table -- a stronger reason than "chat memory is not server-readable", and one that survives even if Claude's memory improves.

**Justification.** The gap is real but narrower than stated, and the fix can be lazier than proposed. One mutable `notes` table with `(date, kind, text, source, expires_at)` carries the chat-sourced constraint **and** the availability one-off, which shrinks Stage 6's planned `availability_rules` table to the recurring work pattern -- three constants that already live in `config/training.ts`, not a table at all. Net: one table added, one avoided. Being on the mutable side of the schema's split, it needs no guard work.

**Action.** Add a single mutable `notes` table to `src/db/schema.ts` (`date, kind, text, source, expires_at`) and a `rocket_add_note` tool; have `get_status` return non-expired notes. Strike `availability_rules` from plan Stage 6 and state that `notes` carries one-off overrides.

<a id="f28"></a>

### F28. The propose/validate contract on every write tool -- ACCEPT

**Verification.** `docs/specs/04-mcp-surface.md:30` carries the intent as prose -- "Tools never dead-end: guardrail refusals always include a compliant counter-offer" -- and nothing else. No `applied` field, no `violated_rules`, no `resulting_window` anywhere in `docs/` or `src/`. `adjust_session` at `:17` is described only as "Guardrail-checked", with no statement of what scope is checked; `docs/specs/03-planner.md:17` says replans edit the rolling window but never says a write revalidates it. Plan Stage 7 (`:524-548`) lists five RED tests, all asserting narration quality, none asserting that a rejected write is not narrated as applied. The review's supporting claim that claude.ai supports neither MCP elicitation nor sampling could **not** be verified here -- but the design does not rest on it, since carrying the question in the tool result is correct either way.

**The other side.** The review supplies no counter, so: "the diff already tells Claude what happened; a boolean is redundant." It is not. A diff containing zero changes and a diff that was refused serialise to the same shape, and the failure mode -- Appendix A's top-ranked risk, with Strava's Athlete Intelligence congratulating a user after a car crash as the public precedent -- is exactly a model filling that ambiguity with optimism. One boolean makes the failure assertable by a test; prose cannot be asserted. The whole-window half is the part that genuinely cannot be retrofitted: if `adjust_session` ships validating one session, widening it later changes the planner's call graph, not a field.

**Justification.** The highest-value item in the review and among the cheapest -- spec text plus a return type, both authored before a single tool exists, and the seam that makes "discussions with Claude shift my training" safe rather than merely conversational. It unlocks F29's promptfoo tier, which has nothing to assert without it, and it subsumes S6.9's rules registry via `violated_rules[]`. It survives every deferral: not integration work, not auth, not a load model.

**Action.** Add the contract to `docs/specs/04-mcp-surface.md` now: every write tool returns `{applied: boolean, violated_rules: [rule_id], compliant_alternative, resulting_window}`, and state explicitly that validation scope is the whole rolling window for every write including `adjust_session`. Add one Stage 7 test: a rejected write returns `applied: false` and the transcript does not claim application.

<a id="f29"></a>

### F29. Evals: fixtures first, transcript assertions second -- ACCEPT

**Verification.** Plan Stage 7's RED block (`docs/plans/PLAN-2026-001-m1-core-loop.md:527-540`) contains exactly five `it(...)` cases: two Loop A (spanner), two Loop B (DOMS), one narration. Race-added and race-cancelled are absent. The gap is not hypothetical -- the header above `RACES` in `config/training.ts` records that the 2026-09-06 macro re-derivation placed the peak long run on Lincoln Half day and a 26 km week ending on LDNX 10K day, uncaught, "because BLOCK held only the goal and tune-up races and no other race existed anywhere the planner could see". Race-cancelled now has a live instance: Dorney carries `role: 'dropped'` with `LIVE_RACE_DATES` filtering it out.

**The other side.** "Promptfoo is a second harness stood up for five to ten cases; the fixture tier is 90% of the value, cut the rest." That partially wins and is honoured. The fixture tier is free and mandatory. The promptfoo tier has exactly one job the fixtures cannot do -- asserting that Claude does not narrate a rejected write -- and that assertion is only writable if F28's `applied` boolean exists. So promptfoo is conditional on F28, not independent of it. LLM-as-judge is cut outright, agreeing with the review.

**Justification.** Accepted and strengthened rather than merely accepted: the two missing scenarios are not speculative coverage, they are a regression test for a bug that already shipped once in this repo and a decision already taken once (Dorney). The fixture pair lifts straight out of `RACES` as it stands, which makes this close to free.

**Action.** Add "race added mid-block" and "race cancelled mid-block" to the Stage 7 fixture list, seeded from `RACES` (Lincoln and LDNX as added; Dorney's `role: 'dropped'` as cancelled). Sequence promptfoo after F28 lands and cut it if F28 slips. Cut LLM-as-judge.

<a id="f30"></a>

### F30. Two honest options for the 48 days -- ALREADY-DONE

**Verification.** Luis chose option A on 2026-09-07. Assessing A's contents rather than re-asking: its largest single cut -- "five Neon tables with ONE append-only layer, app role SELECT/INSERT plus one `ENABLE ALWAYS` trigger, skip event triggers and the two-store contract suite" -- was already delivered in exactly that shape, in `9153fde`, **before** the decision was taken. `src/db/schema.ts` is exactly five tables; `0001_append_only_guards.sql` delivers the grant layer plus `ENABLE ALWAYS` triggers and explicitly rejects the `sql_drop` event trigger at `:30-37` with reasoning. What remains uncut in the plan: Stage 3's second store implementation and contract suite, and Stage 5's OAuth server.

**The other side.** Option B -- a hand-run block with a Claude Project, Strava's connector and intervals.icu -- delivers the coaching value now at zero build risk. It loses on one verified ground: `docs/specs/02-load-engine.md:26` requires prediction-versus-outcome pairs to calibrate the multipliers, and a hand-run block produces none of them. B's real cost is not "later" -- it is that the next block also starts from zero calibration data, so B is not a delay, it is a repeat. Meanwhile A's bill has already partly been paid, a fact the review could not have known.

**Justification.** The decision is taken, so ALREADY-DONE rather than ACCEPT. The residual work is honest bookkeeping: the plan text still contains the stages A cuts, and an agent following the plan document as written will build them. A deferral that lives only in a chat decision and not in the plan file is the deferral-discipline failure mode.

**Action.** Edit `docs/plans/PLAN-2026-001-m1-core-loop.md`: strike Stage 3's second store implementation and contract suite, strike Stage 5 (OAuth 2.1), and record both plus the two-component model in a **Deferred-Items Register** with owner and re-ratification date. Note in the plan that Stage 1's DB scope was delivered at option-A depth in `9153fde`.

<a id="f31"></a>

### F31. What "strong foundation" should mean here -- ACCEPT-MODIFIED

**Verification.** The claimed tension with the "MVP must accommodate ALL data" instruction is not real -- the two are on different axes, and the repo already resolves it. `src/db/schema.ts:16-22` states "Everything Garmin returns is kept: the queryable fields are typed columns, and the whole payload also lands in `raw` so a field we did not think to type is not lost and does not need re-ingesting later", implemented as `raw: jsonb`, with one measured exception (per-second streams at 587,968 bytes against a 0.5 GB tier, held as `streams_ref`). Testing item (b) against the code: grepping `docs/`, `config/` and `src/` for `applied_rules|rules table|rules registry|citation` returns **zero** hits. Item (b) is the only one of the four with neither an implementation nor a plan stage.

**The other side.** REDLINES rule 2 requires append-only "enforced in Postgres, not by convention", which reads as a mandate for guard depth. Engaging it: `0001_append_only_guards.sql` already satisfies the redline with better reasoning than the review's. The review says "ONE append-only layer" while describing two mechanisms; the migration's header explains why one is genuinely insufficient -- the privilege revoke is void against the table owner, who can re-grant to itself, so the `ENABLE ALWAYS` trigger is not depth, it is the layer that binds the other actor. It then adds two TRUNCATE statement-level triggers on live evidence (owner TRUNCATE succeeded on Neon PG 18.6 until they existed). Evidence-driven, not ceremony; the concern is discharged.

**Justification.** Adopt (a)-(d) as the working definition, add the clause the review omitted, and re-rank. "Accommodate all data" is a **capture** requirement -- never need to re-ingest -- and one jsonb column satisfies it; the review's definition is about guard depth and auth ceremony, which the instruction never implied, so the review's list is incomplete rather than in conflict. The re-rank matters more than the addition: the review points at DB guards as the over-build, but the guards shipped correctly and cheaply, while (b) -- rules as cited data -- has nothing behind it at all. The weakest part of the foundation is the part nobody has started, not the part someone over-built.

**Action.** Restate the MVP in the plan against (a)-(e), where (e) is lossless capture of upstream payloads with a named measured exception. Flag (b) as the foundation's only unstarted pillar and fold it into F28's `violated_rules[]` rather than a separate workstream.

<a id="f32"></a>

### F32. The earlier attempt -- ACCEPT-MODIFIED

**Verification.** marathonApp confirmed: `src/data/trainingPlan.js` is 137 lines with 47 `day:` keys (the review said 46 -- immaterial), three localStorage hooks (`useCompleted.js`, `useRunLogs.js`, `useSessionNotes.js`), plus RunLog, SessionNotes, PaceCalculator and NutritionPlanner components -- so it was a static viewer **with** a run log and notes, slightly more than the review's characterisation. Rocket's page: `src/app/page.tsx:2` imports `../data/recent-activities.json`, `:207` footer reads "Not live yet ... a snapshot taken on 2026-09-06, not a running sync", and it has no toggles, no log and no notes. On the page dimension rocket is currently **less** than marathonApp. Post-review, `9153fde` added what marathonApp never had: a schema and a Postgres-enforced append-only history.

**The other side.** "The page is irrelevant -- `docs/specs/00-overview.md:15` makes MCP tools primary and the PWA explicitly secondary and read-mostly, so judging rocket by its home page is judging the wrong surface." That partially wins, and is why this is ACCEPT-MODIFIED: the architectural difference from marathonApp already exists on disk. It only partially wins, because `00-overview.md:19` makes the subjective check-in a first-class signal "shipped before any API integration", and a read-only page cannot take a check-in. The page must become writable no matter how good the tools are. So the right gate is sharper than the review's: not "reads from Postgres" but "accepts a check-in".

**Justification.** The warning is fair and its facts hold, but as written it is a vibe with no failure condition, which means it can be nodded at and never tested. Worth converting into something greppable and dated. `src/data/recent-activities.json` is the tell: while that file is the page's data source, rocket is a viewer.

**Action.** Record a dated gate: `src/data/recent-activities.json` is deleted and `src/app/page.tsx` both reads activities from Postgres **and** accepts a check-in write, before `CHECK_IN_GATES[0].afterWeekMonday` (2026-09-14). If that date passes with the JSON snapshot still wired in, F32 is proven and scope should be cut further.

<a id="f33"></a>

### F33. Personal place name on the branch -- ACCEPT

**Verification.** Still present, verbatim. `docs/specs/03-planner.md:11` on the branch reads "Run-commute slots (Colindale->City ~15km, or partial) are the preferred vehicle for easy volume **if enabled** in availability". Main's line 11 reads "Run-commute slots (home->office, ~15km, or partial) are a supported vehicle for easy volume **if enabled** in availability. Currently disabled: not feasible without shower/changing facilities (ratified 2026-08-18)." A repo-wide grep for the place name and its neighbours across `*.md`, `*.ts` and `*.json` returns exactly that one line (the only other hit is a base64 hash in `package-lock.json`).

**The other side.** Trivial -- a private repo, one place name, and the fix arrives free with the F3 merge. That understates it. `65fb66b`'s commit message gives the reason: the specs named a home-to-work route and exact working hours, and combined with a training schedule that describes where someone lives, works and when they are out of the house -- "permanent once it is in a public git history". Main now carries an MIT licence, i.e. the repo is being prepared to go public. The branch line is also stale training content independent of privacy -- it calls run-commuting "the preferred vehicle", while the plan at line 164 says the targets are reachable without it and that this line should be amended.

**Justification.** Confirmed unfixed and not purely cosmetic: both a privacy leak into a history intended to go public and a stale planning assertion the plan already contradicts. It resolves as part of the F3 merge conflict on this exact file -- but only if the conflict is resolved toward main, which is worth stating explicitly rather than assuming.

**Action.** Resolve the `03-planner.md` merge conflict (F3) by taking main's line 11 wholesale -- the generalised home->office wording plus the "currently disabled, ratified 2026-08-18" clause. Do **not** hand-edit the place name out of the branch's version and leave the "preferred vehicle / biggest lever" framing standing; main's wording fixes both at once.

<a id="f34"></a>

### F34. Probe caches gitignored -- ALREADY-DONE

**Verification.** Confirmed clean. `git check-ignore -v` returns `.gitignore:50` (`tools/**/.venv/`) for `tools/garmin_probe/.venv` and `.gitignore:47` (`__pycache__/`) for `tools/garmin_probe/__pycache__`; both exist on disk. `.pytest_cache` is **not** matched by the repo `.gitignore` (check-ignore exits 1), but it exists at `tools/strava_probe/.pytest_cache` and `tools/garmin_probe/.pytest_cache` and neither appears in `git status --porcelain` -- because pytest writes its own `.pytest_cache/.gitignore` containing `*`. `.gitignore:49` also covers `tools/**/out/`, the higher-stakes path (probe output: health data and live tokens).

**The other side.** Add a `.pytest_cache/` line anyway, because the current protection is second-hand and depends on pytest choosing to write its self-ignore -- run with `-p no:cacheprovider`, or on an older pytest, and the directory becomes untracked and committable by the next `git add -A`, which per F4 has actually happened in this repo. Genuine, and it costs one line. It loses on the merits: the cache holds no secrets and no health data (test-node ids and a `CACHEDIR.TAG`), the CI job runs pytest through `uv run` with default plugins, and adding a line to defend against a flag nobody passes is the redundancy the repo's own discipline argues against.

**Justification.** The finding asked for confirmation, not a change. Confirmation passes: nothing is tracked, nothing is untracked-and-dangling, and the sensitive path is explicitly covered.

**Action.** None. Recorded here so a future reader does not re-open it: `.pytest_cache` is covered by pytest's self-written `.gitignore`, not the repo's.

<a id="h1"></a>

### H1. homescreen-app task file unchecked while its files sit uncommitted -- ALREADY-DONE

**Verification.** Resolved by two commits landed after the review. `tasks/homescreen-app.md` now has all nine checklist items marked `[x]` (extractor script, committed snapshot, manifest/icons/apple-touch-icon, layout metadata, the page, current-week selection with boundary tests, browser-verified at iPhone width, decision-log entry, gates green), and its `## Commits` section records `866f7b6 feat(app): the block on the home screen`. The files are committed and tracked -- `src/app/page.tsx`, `src/app/layout.tsx`, `src/app/manifest.ts`, `src/app/apple-icon.png` and `src/data/recent-activities.json` all appear under `find src -type f` with a clean git status. The task-file record was itself committed as `190d902`.

**The other side.** A hostile reading: the checklist claims "Browser-verified at iPhone width" and "Gates green", and neither was verified here -- what was checked is that a box is ticked, not that the thing behind it happened, which is precisely the verified-not-claimed failure the ci-gates ledger exists to prevent. Fair, and worth naming. It does not change the disposition, because the bullet asked a narrower question -- is the task file reconciled with its files -- and that is mechanically true now.

**Justification.** The bullet's exact remedy ("either commit under that task or record why not") has been done. There is nothing left of the finding as written.

**Action.** None. Flagged for anyone who wants it: the iPhone-width and gates-green ticks on `tasks/homescreen-app.md` are unverified by this review, and auditing checkbox truth rather than checkbox count is a separate pass.

<a id="h2"></a>

### H2. Confirm `ci:harness-tests` exists in the branch's check workflow -- ALREADY-DONE

**Verification.** The job exists. `.github/workflows/check.yml` defines a `harness-tests` job (last in the file) that checks out, sets up uv via `astral-sh/setup-uv` pinned to `f94ec6b`, and runs `uv run --directory tools/garmin_probe pytest -q` and `uv run --directory tools/strava_probe pytest -q`. `docs/ci-gates.md:50` and `:51` both cite it as the enforcer, and line 51 is honest about its limit: "IMPLEMENTED (harness only)... Not yet enforced on the production sync path, which does not exist."

**The other side.** The ledger names the gate `ci:harness-tests` while the workflow job id is `harness-tests` -- GitHub's required-status-check name would be the latter, so branch protection configured off the ledger's string would protect nothing. Against, and this wins: every row in that ledger uses the `ci:` prefix as a namespace for "a job in the check workflow", not as a literal check name, and no branch protection is configured on this repo yet. Renaming to match would be churn on an internally consistent convention.

**Justification.** The bullet asked for confirmation and it passes -- the job is real, SHA-pinned, and runs both probe suites, matching what the ledger claims. The naming mismatch is cosmetic under the ledger's own convention and has no live consumer.

**Action.** None now. If branch protection is ever enabled, take required-check names from the workflow job ids (`harness-tests`), not from the ledger's `ci:`-prefixed labels. Note that F17 deletes `tools/strava_probe/`, so this job loses one of its two commands in the same change.

<a id="h3"></a>

### H3. `~/dev/routr/artifacts/fit_crop/` is the only local FIT history -- ACCEPT

**Verification.** Still unbacked. The directory holds 31 files and 5.9 MB. `git check-ignore -v` in `~/dev/routr` returns `.gitignore:63:artifacts/*`, and `git ls-files artifacts/fit_crop/` returns zero tracked files -- invisible to git, and removed by `git clean -fdx`. No copy exists elsewhere: `find ~ -maxdepth 3 -iname '*fit_crop*'` outside routr returns nothing, and there is no `~/backups`. Two numbers in the docs are stale: `docs/STARTUP_ACCESS.md:71` says "18 files and 5.9 MB", the review says "~34 files", the actual count today is 31.

**The other side.** Two counters. `git clean -fd` does not delete ignored files -- it takes `-x` to reach them, so the stated trigger is slightly overstated. And these are cropped derivatives whose sources may still exist in Strava or Garmin, so losing them costs a re-crop rather than the history itself. Neither rescues the finding. Cropping is lossy and manual -- routr's naming convention encodes post-crop distance, so each file is the output of a decision that would have to be re-made -- and `git clean -fdx` is an ordinary command to run in a repo with twelve worktrees, one of which has an agent active in it right now. 5.9 MB of irreplaceable-by-effort personal training data with zero copies is not worth arguing about at this price.

**Justification.** Accepted unchanged, and the only item in this review that can destroy something. It is also the cheapest to close: one copy command. It sits outside rocket's repo boundary, which is exactly why it keeps not getting done -- no task file owns it. That is a reason to name an owner today, not to defer.

**Action.** Copy the directory out of the repo now -- any path outside `~/dev` -- and correct `docs/STARTUP_ACCESS.md:71` to say 31 files and `git clean -fdx` rather than 18 files and `git clean`. Luis's hands or a rocket-side agent; do **not** run it from inside `~/dev/routr`, which is read-only with an active agent.

<a id="s61"></a>

### S6.1. Single-session spike guardrail -- ACCEPT

**Verification.** `GUARDRAILS` contains `rampCapPct` 15, `aggressiveRampCapPct` 35, `rampMode`, `returningFromRestRampCapPct` 35, `highVolumeThresholdKm` 80 / `minRunDaysAtHighVolume` 6, `minRestOrSwimOnlyDaysPerWeek`, `maxQualitySessionsPerWeekBuild`, `minDaysBetweenQualitySessions`, `protectedTaperWeeks`. No `singleSessionSpikePct` and no trailing-30-day term anywhere.

**The other side.** The 110% figure is a population hazard ratio, not a hard cap, and a strict version makes a 33 km long run unreachable from a 21 km start. That does not defeat adding the threshold -- it defeats enforcing it silently. The repo's own pattern already handles this case: `docs/specs/03-planner.md:28` requires a breach to be named and costed rather than executed, and weeks already carry `rampExemption` strings for exactly that.

**Justification.** The gap is confirmed and the guardrail is on the wrong axis as claimed. The number and the peak-long-run override are training judgements, and the ramp logic is Stage 6 work option A has not reached. Add the field and the measurement; the ratification and the specific override are Luis's.

**Action.** Add `GUARDRAILS.singleSessionSpikePct` measured against the trailing-30-day longest completed run, and carry the peak long run as a named `rampExemption`. Threshold value and the override itself: **NEEDS-LUIS**, section 4.

<a id="s62"></a>

### S6.2. Workouts pushed to the watch -- ACCEPT

**Verification.** Grepping `docs/` for "to the watch", "workout push", "push the week" returns nothing. intervals.icu appears only as an **ingest** bridge (`docs/specs/05-integrations.md:29-31`, `docs/plans/PLAN-2026-001-m1-core-loop.md:241-249`) -- a source, never a sink. `docs/specs/07-wiring-todo.md` lists six milestones M1-M6, none of which is workout delivery.

**The other side.** "The plan is a phone app and a chat; the watch already has Garmin's own workouts, so pushing is duplication." That loses on the product's own premise: if the plan adapts daily but the watch shows yesterday's session, the athlete follows the watch and the adaptation is decorative.

**Justification.** Confirmed absent, and the intervals.icu decision has quietly made it cheap -- the same personal API key that ingests wellness can push a planned workout, so this is one endpoint on a job that has to exist anyway, not a new integration.

**Action.** Add workout push to `docs/specs/05-integrations.md` as a sink on the intervals.icu bridge, and make it a step of the daily job rather than its own milestone. Scope per F19: a calendar event, not a compiled structured workout.

<a id="s63"></a>

### S6.3. `notes` table and `rocket_add_note` -- ACCEPT-MODIFIED (duplicate of F27)

**Verification.** See [F27](#f27). Five `pgTable` calls, no `notes`; `01-domain-model.md:30` already models the review's own "Leeds Thursday" example as an `AvailabilityRule` override.

**The other side.** Same as F27: `check_ins` already has a free-text note. It cannot serve, because `check_ins` is append-only under `0001_append_only_guards.sql` and keyed to a day that has happened, while a constraint about a future date must be mutable and revocable.

**Justification.** Duplicate of F27, carried so the section-6 list has a verdict per row. The modification is the same: one mutable `notes` table absorbs availability one-offs so Stage 6's `availability_rules` table is never built.

**Action.** As F27. Do not track this as a second work item.

<a id="s64"></a>

### S6.4. applied/rejected/alternative contract -- ACCEPT (duplicate of F28)

**Verification.** See [F28](#f28). `docs/specs/04-mcp-surface.md:30` carries the intent in prose only; no structured field exists anywhere; no plan test asserts non-narration of a rejected write.

**The other side.** As F28: the diff is not a substitute, because a zero-change diff and a refused diff are indistinguishable in shape, and that ambiguity is precisely where the documented failure mode lives.

**Justification.** Duplicate of F28, carried for list completeness. Highest value and among the cheapest in the whole section-6 list, and the one that unlocks S6.9 and F29's promptfoo tier.

**Action.** As F28. Do this before any tool is implemented.

<a id="s65"></a>

### S6.5. Proactivity: daily cron plus iOS Web Push -- ACCEPT-MODIFIED

**Verification.** Partly stale. Web Push is not unspec'd -- `docs/decisions.md:451-473` is a dated ACTIVE decision that ships the home-screen app without a service worker and explicitly defers web push, with reasoning (iOS 26 removed installability requirements; Serwist is webpack-only against rocket's Turbopack). Cron reliability is characterised at PLAN `:64` (Vercel Hobby: 1x/day, ±59 min, never retried, can be missed with no log) and `:647` (a healthchecks.io dead-man's switch). Genuinely absent: any spec of what the daily pass **does**, and any `coach_notes` table -- grepping `docs/`, `config/` and `src/` for `coach_note|daily pass|proactive` returns nothing.

**The other side.** "Push is deferred by a recorded decision, so the item is closed." Half right, and the review did not know it. But the deferral covers the **delivery channel**, not the daily computation. A cron that ingests, recomputes and evaluates replan triggers is valuable with no push at all; its output can simply be waiting in `get_status` when the next conversation opens. Collapsing the two loses the half that survives.

**Justification.** Narrow the claim rather than accept it whole. The push half is decided and dated; re-raising it would re-litigate a recorded decision. The daily-pass half is real, is on option A's own critical path ("daily cron ingesting intervals.icu"), and has no spec describing its steps or its output.

**Action.** Spec the daily pass in `docs/specs/05-integrations.md`: ingest -> recompute -> evaluate replan triggers -> write a coach note, with the healthchecks.io dead-man's switch. Leave web push deferred per `docs/decisions.md:451`. Add a `coach_notes` table only if the daily pass needs to persist prose; otherwise fold its output into `notes` (S6.3).

<a id="s66"></a>

### S6.6. Goal as a range, surfaced in `get_status` -- ACCEPT-MODIFIED

**Verification.** Partly landed. `PACE_ESTIMATES.planningBandSeconds` already carries `{fast: 13800, slow: 14400}` with its derivation (Garmin's half-to-marathon ratio 12940/5892 applied to a 1:45 half, degraded for only four long runs). Not landed: `docs/specs/06-training-block.md:3` still reads "as fast as possible" and `:16` still reads "Implied goal band ~3:25-3:35", and the SUPERSEDED banner at `:5-13` scopes itself to "dates and volumes" so it covers neither. `docs/specs/04-mcp-surface.md:7` has `get_status` returning "days to next race + goal race" -- the race, not the goal time or band.

**The other side.** "The band is provisional until 12 September, so writing it into the specs now creates a second stale number to fix next week." That argues for sequencing, not omission -- and it argues against leaving 3:25-3:35 in place, since a stale number that contradicts the live config is worse than a provisional one that matches it. The band is also structurally right regardless of the result: a range is what `get_status` should return whatever number goes in it.

**Justification.** Luis's 2026-09-07 decision retires "as fast as possible" as a planning input, which makes `06-training-block.md:3` and `:16` directly contradict a ratified decision. That is a one-line fix with a decision behind it, not a judgement call. The `get_status` half is a spec addition with no implementation cost today.

**Action.** Replace `06-training-block.md:3` and `:16` with a pointer to `PACE_ESTIMATES.planningBandSeconds`, and widen the SUPERSEDED banner to cover the goal. Add the band to `get_status` in `04-mcp-surface.md`. Schedule re-derivation of `planningBandSeconds` on 2026-09-12 from the actual half result (F5 action 3).

<a id="s67"></a>

### S6.7. Swim end-time as a readiness input -- ACCEPT-MODIFIED

**Verification.** `docs/specs/02-load-engine.md:11-13` covers swim as real stress with a near-zero musculoskeletal component, and `:19-24` defines readiness inputs. Nothing anywhere references the swim session's **end time**. `AVAILABILITY` has `swimSessionsPerWeek: 1` and `swimSessionHours: 2`, but no time of day.

**The other side.** "Late swim compressing sleep is already captured -- the check-in asks about sleep and Garmin measures it, so a derived proxy double-counts a directly measured signal." That largely wins for the readiness model itself: measured sleep beats inferred sleep, which is the repo's own live-data-beats-theory stance. What it does not win is the data question -- the swim slot's end time is recorded nowhere, so if the term ever proves useful it cannot be evaluated retrospectively.

**Justification.** Downgrade rather than accept. A readiness term here is speculative and duplicates a measured signal, and readiness is Stage 9 work deferred past the race anyway. But capturing the end time is one constant in config, costs nothing, and preserves the option. Capture now, model never unless the data argues for it.

**Action.** Add the swim slot's start and end time to `config/training.ts` alongside `swimSessionsPerWeek`. Do **not** add a readiness term. Revisit only if measured sleep on swim nights diverges from other nights.

<a id="s68"></a>

### S6.8. Race-added and race-cancelled fixtures -- ACCEPT (duplicate of F29)

**Verification.** See [F29](#f29). Plan Stage 7 has five tests, none covering races. Both scenarios have concrete instances in `config/training.ts` `RACES` today -- the uncaught long-run-on-Lincoln-Half-day placement recorded in the `RACES` header, and Dorney's `role: 'dropped'`.

**The other side.** "These are planner behaviours Stage 6 will cover generically once races are a first-class constraint, so a dedicated fixture is redundant." It is not: the bug already happened once with races present in a spec and absent from the config the planner reads. A generic constraint mechanism did not prevent it; a fixture asserting no session lands on `LIVE_RACE_DATES` would have.

**Justification.** Duplicate of F29, carried for list completeness, and the strongest-evidenced item in the section-6 list -- a regression test for a defect this repo actually shipped, not speculative coverage.

**Action.** As F29. Seed both fixtures from `RACES` as it stands.

<a id="s69"></a>

### S6.9. A rules registry returned as `applied_rules[]` -- ACCEPT-MODIFIED

**Verification.** Grepping `docs/`, `config/` and `src/` for `applied_rules|rules table|rules registry|citation` returns **zero** hits -- no registry in any form. But `config/training.ts` already carries the substance in prose: `GUARDRAILS.aggressiveRampCapPct` documents why 35 replaced 15 and who ratified it; `CHECK_IN_GATES[0]` criteria cite Lolli 2019 on mathematical coupling and Impellizzeri 2020 on the acute-to-random ratio, with an explicit instruction to treat acute:chronic as a trend and never a threshold.

**The other side.** The review's framing implies a rules **table**. For one athlete with roughly ten thresholds that already live in a typechecked, commented, git-versioned config file, a table is a second home for the same constants and a migration every time a number changes -- the config **is** the registry, and it is better at citation than a table, because prose comments carry reasoning a `citation` column cannot. What the config genuinely cannot do is name itself in a tool response: there are no stable ids, so a planner decision cannot say which rule it applied.

**Justification.** **Reject the table, accept the mechanism.** The gap is not storage, it is emission -- and F28's `violated_rules[]` already requires stable rule ids to exist. Adding an `id` to each `GUARDRAILS` entry and emitting `applied_rules[]`/`violated_rules[]` delivers the entire checkability claim with no table, no migration and no second source of truth. The lazier and the more correct answer.

**Action.** Give each `GUARDRAILS` entry a stable string id in `config/training.ts`; have the planner emit `applied_rules[]` and `violated_rules[]` as rule ids on every decision, per F28. Do not create a rules table.

<a id="s610"></a>

### S6.10. Under- and over-performing detection -- ACCEPT

**Verification.** Confirmed exactly as stated. `docs/specs/03-planner.md:15` lists trigger 2 as "Ad-hoc activity logged that deviates >20% from plan (the spanner)", implemented as `REPLAN.spannerDeviationPct: 20` -- a single-activity threshold. No rolling comparison of completed volume or pace against `PACE_ESTIMATES` or week `targetKm` exists anywhere in `config/`, `docs/specs/` or `src/`. The nearest thing is `CHECK_IN_GATES[0]`, whose days-hit and acute-chronic-trend criteria cover some of this ground but are a manual weekly question put to a human, not detection.

**The other side.** "`CHECK_IN_GATES` already covers it -- a human reviews the week and decides whether the next step up is earned, which is more honest than an automated trend that will fire on noise." That wins on the guardrail axis: the gates are the right mechanism for deciding whether to escalate volume, and automating that decision would be worse. It does not cover the other direction. The gates fire only at authored checkpoints and ask only whether the plan was **delivered**; nothing notices that the plan is being delivered comfortably and the paces are ahead of `PACE_ESTIMATES`, which is the over-performing case and the one that should revise the goal band rather than hold volume.

**Justification.** Accepted, and it ranks above most of this list because Luis named it and because it feeds the goal band (S6.6) -- an over-performing trend is exactly the evidence that should move `planningBandSeconds` after 12 September, and there is currently no mechanism that would notice. The cheap version needs no new table and no new tool.

**Action.** Add one function comparing trailing-14-day completed km against week `targetKm`, and mean easy pace against `PACE_ESTIMATES`, surfaced in `get_status` as a two-value trend (volume, pace) with a direction. No new table, no new tool, no automatic replan -- it informs the check-in gate and the goal band rather than acting on its own.

---

## Consolidated actions

Ordered: already delivered, do now, deferred past 2026-10-24, needs Luis. Nothing in this ledger is filed as "consider later" -- per the deferral-closure rule, every deferred row names an owner and a re-ratification date, and any row that cannot be classified belongs in the do-now list instead.

### Already delivered (evidence, not assertion)

| Finding            | What landed                                                                                                                                                                                              | Evidence                                                                 |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| F6, F29 seed       | Races as first-class data: `RACES`, `LIVE_RACE_DATES`, `longRunDate`/`longRunOnRace` on every week, Lincoln carrying week 4 as marathon pace inside a ~35 km day                                         | `b6ea990`, `b026fb9`                                                     |
| F30, F31           | Five Neon tables at option-A depth, one grant layer plus `ENABLE ALWAYS` triggers, `sql_drop` event trigger explicitly rejected with reasoning; lossless `raw: jsonb` capture with the streams exception | `9153fde`, `src/db/schema.ts:16-22`, `0001_append_only_guards.sql:30-37` |
| F24, H1            | Home screen shipped without Serwist and without a service worker; task file reconciled with its files, all nine items closed                                                                             | `866f7b6`, `190d902`                                                     |
| F4 (gate half)     | Task-trace gate returned to green; `check_task_trace.py --range main..HEAD` silent at `b026fb9`                                                                                                          | run at authoring time                                                    |
| F8 (evidence half) | Weeks 3 and 4 `UNRATIFIED` exemptions replaced; the block's one override is week 2's, marked RATIFIED and asserted by test                                                                               | `8385910`                                                                |
| F21, F34, H2       | Bulk-export source, probe gitignores and the `harness-tests` CI job all confirmed correct as they stand                                                                                                  | verification above                                                       |

### Do now

| #            | Action                                                                                                                                                                                                                                                          | Owner file / task                                                  |
| ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| F3, F33      | Merge main into `feat/m1-core-loop`, resolving both conflicts by hand: main's line 11 wholesale in `03-planner.md`; branch structure minus the Dorney line in `01-domain-model.md`. Recovers LICENSE, README and the vitest worktree exclusion                  | branch, coordinated with `race-aware-block`                        |
| H3           | Copy `~/dev/routr/artifacts/fit_crop/` (31 files, 5.9 MB) outside `~/dev`; correct `docs/STARTUP_ACCESS.md:71` to 31 files and `git clean -fdx`                                                                                                                 | Luis or a rocket-side agent -- never run from inside `~/dev/routr` |
| F17          | Delete `tools/strava_probe/` and `docs/STRAVA_SETUP.md`; revoke the Strava API app and the two `pass` entries; rewrite `05-integrations.md:54`; amend `tasks/integration-harness.md`; decision entry citing §5.3                                                | `integration-harness`                                              |
| F28, S6.4    | Write the write-tool contract into `docs/specs/04-mcp-surface.md` -- `{applied, violated_rules[], compliant_alternative, resulting_window}`, whole-rolling-window validation on every write including `adjust_session`                                          | spec                                                               |
| F26          | Rename all eleven tools in `04-mcp-surface.md` to the `rocket_` prefix, before the connector fixes the names                                                                                                                                                    | spec                                                               |
| F5, S6.6     | `06-training-block.md:3` and `:16` -> pointer to `PACE_ESTIMATES.planningBandSeconds`, Battersea named as the settling event, banner widened; `CLAUDE.md` goal line amended; band added to `get_status`                                                         | spec + CLAUDE.md                                                   |
| F14          | Withdraw PLAN `:151` open-question-4 recommendation; add the trend-display paragraph to `02-load-engine.md`; fix `:17` so the ramp input is weekly run km; NOT IMPLEMENTED row for "no decision reads TSB"                                                      | plan + spec + ci-gates                                             |
| F8           | Delete `GUARDRAILS.returningFromRestRampCapPct`, rewrite its test against `ACTIVE_RAMP_CAP_PCT` + the RATIFIED marker, close the 2026-08-15 decision as SUPERSEDED, remove the Stage 6 blocker                                                                  | `config/training.ts` owner + plan                                  |
| F12          | `objectiveShareWhenAvailable` 0.4 -> 0.3, `objectiveCanOnlyDowngrade: true` with the `min()` formula in the docstring, `hrv` shape block, probe-evidence note; mirror into `02-load-engine.md:22-24`; test asserting the share is <= 0.3                        | `config/training.ts` owner                                         |
| F7, S6.1     | Add `GUARDRAILS.singleSessionSpikePct` measured against the trailing-30-day longest completed run, as a costed advisory with `LIVE_RACE_DATES` exempt; reshape the ladder to 22/27/33                                                                           | `config/training.ts` owner (threshold value: Luis)                 |
| F9           | Add the "longest single run stays ~32 km, days-out stated in the week note" acceptance criterion to `tasks/race-aware-block.md`                                                                                                                                 | `race-aware-block`                                                 |
| F15          | Keep `maxQualitySessionsPerWeekBuild` at 1 with both arguments recorded; add `racesCountAsQualitySessions: true` and a test; set week 7 `targetKm`; reword `03-planner.md:26` to name LDNX by date and state taper holds intensity                              | `config/training.ts` owner + spec                                  |
| F16          | Present tense -> designed tense in the `LOAD` docstring and `02-load-engine.md:11`; resolves the contradiction with `07-wiring-todo.md:35`                                                                                                                      | `config/training.ts` owner + spec                                  |
| F13          | One paragraph in `02-load-engine.md` recording the 20:30 swim cutoff rule as inert-until-the-watch-is-worn-asleep; decision entry citing Leota 2025. No column, no readiness term                                                                               | spec                                                               |
| S6.7         | Swim slot start and end time into `AVAILABILITY`. Capture only                                                                                                                                                                                                  | `config/training.ts` owner                                         |
| F10          | REDLINES row: aggregates are computed by code from the full series with the window stated, never derived by the model from a raw list; matching NOT IMPLEMENTED row; link the 2026-09-06 decision entry to it                                                   | REDLINES + ci-gates + decisions                                    |
| F11          | REDLINES row: session and week rows are written only by the deterministic planner; matching NOT IMPLEMENTED row                                                                                                                                                 | REDLINES + ci-gates                                                |
| S6.9         | Stable string id on each `GUARDRAILS` entry; planner emits `applied_rules[]`/`violated_rules[]`. No rules table                                                                                                                                                 | `config/training.ts` owner + spec                                  |
| F30, F2, F31 | Rewrite PLAN-2026-001 for option A: strike Stage 3's second store and contract suite, strike Stage 5, add a Deferred-Items Register with owners and dates, add a one-line wall-clock budget and a cut-line per surviving stage, restate the MVP against (a)-(e) | plan                                                               |
| F23          | Reorder Stage 5 to sit after the planner and check-in loop; record the `static_headers` finding as a decision so the question is not re-asked                                                                                                                   | plan + decisions                                                   |
| F22          | Correct PLAN `:36-37` and `:92` on the SDK/`mcp-handler` facts; pin Stage 4 to `mcp-handler@1.1.0` + `sdk@1.29.0`; delete the Stage 0 SDK spike and zod-3 probe                                                                                                 | plan                                                               |
| F19, S6.2    | "Outbound: the plan on the wrist" subsection in `05-integrations.md` -- one intervals.icu event per day on the existing daily job                                                                                                                               | spec                                                               |
| S6.5         | Spec the daily pass: ingest -> recompute -> evaluate replan triggers -> write a coach note, with the dead-man's switch                                                                                                                                          | spec                                                               |
| F18          | Run the G1 probe and record actual field coverage; capture `dailyTrainingLoadChronic` 287 / `dailyTrainingLoadAcute` 296 as the CTL/ATL seed; amend `tasks/integration-harness.md:15`                                                                           | `integration-harness`                                              |
| F27, S6.3    | Mutable `notes` table (`date, kind, text, source, expires_at`) + `rocket_add_note`; `get_status` returns non-expired notes; strike `availability_rules` from Stage 6                                                                                            | `src/db/schema.ts` + spec + plan                                   |
| F29, S6.8    | Add race-added and race-cancelled to the Stage 7 fixture list, seeded from `RACES`; cut LLM-as-judge                                                                                                                                                            | plan                                                               |
| S6.10        | One trailing-14-day volume/pace trend function surfaced in `get_status`. No table, no tool, no automatic replan                                                                                                                                                 | spec, then Stage 6                                                 |
| F25          | Re-rank the lift table for option A: wiring, utilities, ingest pattern, ICS pair, push stack, OAuth shim last                                                                                                                                                   | plan                                                               |
| F4           | One line in `AGENTS.md` section 3: explicit paths, never `git add -A`, when another agent shares the worktree, with the originating incident appended                                                                                                           | AGENTS.md                                                          |
| F32          | Record the dated gate: `src/data/recent-activities.json` deleted and the page both reading from Postgres and accepting a check-in, before 2026-09-14                                                                                                            | plan                                                               |
| F21          | Record the bulk-export request date in `docs/decisions.md` once submitted; one line in Stage 3b that the bridge covers activities but not wellness                                                                                                              | decisions + plan                                                   |
| F24          | One line in `tasks/homescreen-app.md` recording the verified DHT push port boundary                                                                                                                                                                             | task file                                                          |

### Deferred past 2026-10-24

| #          | Item                                                                          | Classification                                                                                                         | Owner / re-ratification                                            |
| ---------- | ----------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| F23        | OAuth 2.1 server (Stage 5, ~600 lines)                                        | RATIFIED-out by the 2026-09-07 option-A decision                                                                       | Post-race; port DHT's `lib/mcp-oauth.ts`, do not write fresh       |
| F16        | Two-component cardio/musculoskeletal load model (Stage 8)                     | RATIFIED-out; revisit trigger already named in PLAN `:124-139`                                                         | After ~3 weeks of Stage 9 data alongside Garmin's own series       |
| F12 (impl) | Readiness formula (Stage 9)                                                   | RATIFIED-out; the config and spec edits land now, the implementation does not                                          | Post-race                                                          |
| F26        | `docs/specs/08-capability-packs.md` and both the routr and DoHardThings seams | RATIFIED-out; the `rocket_` rename is pulled forward because tool names cannot be changed after the connector is added | Post-race; register row names the 30-minute routr candidate expiry |
| F30        | Stage 3 second store implementation and contract suite                        | RATIFIED-out                                                                                                           | Post-race                                                          |
| F15        | Grade- and pace-dependent descent multiplier                                  | BLOCKED-external: `MULTIPLIERS` has no consumer until Stage 8 exists                                                   | With Stage 8                                                       |
| F13        | Swim-night HRV exclusion implementation                                       | BLOCKED-external: no overnight HRV or sleep score exists in the account                                                | When the watch is worn asleep (`07-wiring-todo.md:22`)             |
| F19        | Structured workout steps, pace/HR targets, Garmin-direct workout upload       | RATIFIED-out; the calendar-event leg ships now                                                                         | Post-race                                                          |
| F29        | promptfoo tier                                                                | MUST-this-sprint, conditional on F28 landing; cut if F28 slips                                                         | Same sprint as Stage 7                                             |

### Needs Luis

Two decisions, both narrow. All four section-8 questions were answered on 2026-09-07 -- races ratified (Dorney dropped, Lincoln and LDNX stand), intervals.icu primary with the Garmin probe as manual fallback, goal as a range with "as fast as possible" retired, and scope option A -- so nothing else in this ledger is blocked on him.

1. **Single-session spike threshold and the peak long-run override (S6.1, F7, extends section-8 Q3).** The evidence says injury risk tracks how far a single run overshoots your longest run of the last thirty days, not how much the weekly total rose -- the largest study to date (5,205 runners, ~600,000 sessions) puts the inflection at about 110%. Under the block as committed, four sessions breach it: the half on 12 September at 132%, the 30 km on 27 September at 142%, the 35 km on 4 October at 117%, and the marathon itself at 121%. The marathon breaching it is unavoidable; the 27 September step is not. Two things to say yes or no to: (a) add the measure at 110% as a **costed warning that can always be overridden**, never a blocker -- the recommendation, because the goal race itself fails a hard version; (b) reshape the long-run ladder from 20/30/35 to **22/27/33**, which keeps every weekly total identical and removes the worst spike, at the cost of two kilometres off the longest run of the block.
2. **Phone access before the race (F23).** Deferring the login server means the coach is reachable from a laptop but not from your phone until after 24 October -- and 06:00 check-ins happen in bed, not at a desk. The ten-line alternative is to publish the connector with no login at an unguessable URL, only reachable from Anthropic's own servers. The risk in one sentence: anyone who obtains that URL and can reach it from Anthropic's network could read your training data (they could not corrupt your history -- the database refuses edits and deletes). Say no and the phone waits until after the race.

Two operational facts to relay, not questions:

- **The Garmin bulk export has not been requested** (F21). Garmin Connect web -> Account Settings -> Export Your Data. Two minutes of clicking, then a 24-48 hour wait, and it is the only sanctioned route to a complete history. The plan says "do this first"; it has not been done.
- **The watch is not being worn asleep** (F18). `validSleep` is false and the HRV endpoints return empty on seven days of probe output, so overnight metrics are absent from **both** candidate data sources. Wearing it overnight is worth more to the readiness model than either integration decision.

---

## Rejections, in full

Nothing below is being actioned. Each is listed so the rejection is as visible as the acceptances, and so a later reader does not re-raise it as an oversight.

| Rejected                                                                                                                                                    | Why                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **F20 in full** -- "Google Calendar is already a connector; the deferred ICS-feed integration is dead weight and Claude should read the calendar directly." | The accepted half changes one already-hedged clause that gates nothing (`05-integrations.md:52` already reads "a later nice-to-have; don't block on it"). The asserted half is wrong about where the planner runs: the deterministic planner is a Vercel cron with no Claude session and therefore no connector access. Acting on it would justify not persisting availability at all, which contradicts the review's own F27. |
| F1's headline -- "zero product code"                                                                                                                        | Stale by eight commits. 1,122 lines of DB layer and home screen are committed; the honest restatement is that the _coach_ does not exist, which F30-F32 already carry.                                                                                                                                                                                                                                                         |
| F2's velocity premise                                                                                                                                       | Falsified: Stage 1 landed in under a day of wall clock. The premise measured a three-week holiday, not a build rate. The ordering half survives.                                                                                                                                                                                                                                                                               |
| F2's requested output -- a day-count for all ten stages                                                                                                     | Waste after the scope cut. A budget for the surviving option-A stages replaces it.                                                                                                                                                                                                                                                                                                                                             |
| F5's evidence about `CLAUDE.md`                                                                                                                             | False. `CLAUDE.md` never carried the 3:25-3:35 band. Its error is different and larger: it still says "as fast as possible", retired as a planning input on 2026-09-07.                                                                                                                                                                                                                                                        |
| F7's "make it the primary guardrail" at 110%, as a cap                                                                                                      | Unimplementable: the goal race is 121-141% of the trailing-30-day longest under every constructible version of this block. And the hazard ratios are non-monotonic (1.64 / 1.52 / 2.28), so band-level tuning is noise -- only the binary carries signal. Adopted as a costed advisory instead.                                                                                                                                |
| The appendix's paired recommendation to cut the weekly cap to 10%                                                                                           | Reverses a decision Luis ratified explicitly on 2026-09-06 through a config edit rather than a conversation.                                                                                                                                                                                                                                                                                                                   |
| F8's NEEDS-LUIS routing and its Stage 6 planner blocker                                                                                                     | The number drives nothing -- `returningFromRestRampCapPct` has no consumer, and the block's one override stands on an explicit ratification that exceeds it anyway. Asking for ratification of dead config spends a scarce decision on nothing. Deleted instead.                                                                                                                                                               |
| F9's "Action: none"                                                                                                                                         | Not safe as written: the F6 fix was in the process of degrading the exact constraint F9 names. Promoted to an acceptance criterion instead.                                                                                                                                                                                                                                                                                    |
| F10's "keep it as the standing example"                                                                                                                     | A no-op that leaves the lesson in an append-only log nobody reads back. Converted into the redline it is evidence for.                                                                                                                                                                                                                                                                                                         |
| F13's cost claim -- the spec treats swimming as near-zero cost                                                                                              | The review read `swimMusculoskeletal: 0.05`, a musculoskeletal multiplier, as a total-cost one. Both `02-load-engine.md:13` and PLAN `:560` already give swim **full** cardio credit.                                                                                                                                                                                                                                          |
| F13's swim-end-time column                                                                                                                                  | `src/db/schema.ts:124` `startTimeLocal` plus `:131` `durationS` already yields it.                                                                                                                                                                                                                                                                                                                                             |
| F15's second weekly quality session                                                                                                                         | Filipas 2022 measured ~1.5% on stable volume in well-trained runners; this block is three build weeks inside a 35->100 km ramp in a new shoe, and Doherty 2020 links marathon outcome to volume parameters, not quality count. The block is not short of intensity -- a maximal half, a marathon-pace half and a raced 10K are already in it.                                                                                  |
| F15's descent-term refinement, pre-race                                                                                                                     | `MULTIPLIERS` has no consumer and Stage 8 is deferred: tuning a number nothing reads.                                                                                                                                                                                                                                                                                                                                          |
| F19's framing -- a `push_session_to_watch` cron stage plus a `rocket_push_week` tool                                                                        | Implies a workout compiler. What earns its place is knowing at 06:00 what today's session is, which is a calendar event and one POST on a job that already has to exist.                                                                                                                                                                                                                                                       |
| F22's premise that the plan is simply wrong about SDK v2                                                                                                    | npm says both are half right: v2 exists as the renamed `@modelcontextprotocol/server`, and v1 is maintained, not frozen. The plan's actual error is elsewhere -- `mcp-handler` was not superseded, it is at 2.1.1 on v2 -- and its unpriced cost is the tool layer, not the SDK.                                                                                                                                               |
| F23's "check the connector dialog for static headers"                                                                                                       | Already answered NO twice on this account, and `static_headers` is an org-admin beta feature by Anthropic's own description. The five-minute check would burn five minutes to confirm what two projects already recorded.                                                                                                                                                                                                      |
| F26's timing for `08-capability-packs.md`                                                                                                                   | Post-race, with one exception pulled forward: the `rocket_` prefix, because tool names are fixed into the connector when it is added.                                                                                                                                                                                                                                                                                          |
| S6.7's readiness term for swim timing                                                                                                                       | Duplicates a directly measured signal (Garmin sleep, the check-in's sleep rating) with an inferred proxy. The end time is captured; no term is added.                                                                                                                                                                                                                                                                          |
| S6.9's rules table                                                                                                                                          | The config file already **is** the registry, and it carries reasoning a `citation` column cannot. The gap is emission, not storage -- solved by stable ids plus F28's `violated_rules[]`, with no table and no migration.                                                                                                                                                                                                      |
| F29's LLM-as-judge tier                                                                                                                                     | Cut, agreeing with the review.                                                                                                                                                                                                                                                                                                                                                                                                 |
| F32's framing as an unfalsifiable warning                                                                                                                   | Converted into a greppable, dated gate instead: the JSON snapshot deleted and a check-in write accepted, before 2026-09-14.                                                                                                                                                                                                                                                                                                    |
| F34's suggested `.pytest_cache/` gitignore line                                                                                                             | The cache carries no secrets and no health data, pytest writes its own self-ignore, and CI runs it through `uv run` with default plugins. A line defending against a flag nobody passes.                                                                                                                                                                                                                                       |
