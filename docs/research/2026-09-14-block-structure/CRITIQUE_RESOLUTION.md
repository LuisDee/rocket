- Coach, major: marathon pace of 5:03 not backed by his results. ACCEPTED. The band is now 5:08-5:15: fast end from his measured Riegel exponent on the official time; slow end +2.3% for trainers and the carbon-shod, carb-loaded half. The anchor moves to the official distance (281.5 s/km). One MP HR ceiling of 168 replaces both 170 and the 172 Lincoln trigger. Lincoln runs progressively (5:12-5:15, then towards 5:05-5:08 only under HR 165). After Lincoln, paces are re-derived as an athlete-accepted proposal. Put to Luis as Q6.
- Coach, major: threshold of 4:29 faster than Daniels' prediction and not reconciled with the earlier 4:52. ACCEPTED. Threshold is now 4:34-4:38: Daniels 60-min pace 4:32, recomputed at VDOT 45.7, plus a 1.5% carbon-shoe allowance (Hoogkamer 2018 verified: energy cost 4.0-4.2% lower). The design states it supersedes 4:52 (July anchor) and 4:29 (GPS-normalised Battersea x July-fitted 0.965). The over-180 HR loop is kept.
- Coach, major: MP ladder 0 -> 8 -> 21.1 then 16 days with none. ACCEPTED in part. 27 Sep MP rises to 10 km; Lincoln is progressive; the Thu 15 Oct threshold touch becomes 6 km at MP; the 3.2 km dress rehearsal stays. REJECTED: the Wed 7 Oct MP option (inside the design's 4 easy days after Lincoln) and the Wed 16 Sep steady finish (inside the 7 easy days after the half, which close Sat 19). The 'only one threshold session' point is recorded but not changed: config's racesCountAsQualitySessions docstring deliberately makes Lincoln spend the 100 km week's budget, to prevent threshold stacked on peak volume. Named as a risk choice.
- Coach, major: 20 days with nothing over 16 km before the marathon. ACCEPTED. Sun 11 Oct becomes 20 km (4 km warm-up + 10K + 6 km cool-down) and Fri 9 Oct drops to 10 km, which also removes that week's double. Put to Luis as Q3. Checked against the transcriptions: Pfitzinger runs 17 mi 14 days out and 13 mi 7 days out; Higdon 12 then 8 mi. The design applies Higdon's 60%/40% of peak to both taper sessions (20/13 km). The resulting advisory against rocket's 13 km fortnight ceiling is named; that ceiling is not a Bosquet or Smyth finding.
- Coach, major: overflow ladder adds doubles its own limits don't need. ACCEPTED, with one sub-point rejected. New order: fill singles to caps, raise recovery days to 10, reopen a run day, then at most one double a week, never on the 2 days before a race or MP long session. Pfitzinger 55-70 plans never have more than one double in a week, and all 8 fall 2 or 4 days after the long run (parsed). Result: 1 double in the block instead of 3. REJECTED: the check that the first double is not in the peak week. Single runs cannot hold 100 km (max 97; 99 without the legs-day cap), so the check would force an unneeded double into week 3. Instead the first double is flagged, placed where Pfitzinger puts his, and removable via Q4. The coach's own example also used that Tuesday.
- Coach, major: heavy legs close to Lincoln and the 10K; the draft's rule was silently overridden. ACCEPTED. Legs is now chosen before the medium day, with a stated rule: at least 3 days after and at least 4 days before any race or MP long session. It uses a maintenance dose (2-3 sets x 3-5, 2 in reserve, no plyometrics or eccentric emphasis) in race, MP-long and post-race weeks. The run goes in the earliest slot, and a note appears whenever legs leaves its first candidate. Resulting days are Sat 19, Wed 23, Wed 30 and Wed 7, all of which meet the gap.
- Coach, minor: race-week monotony counted marathon day as 0. ACCEPTED. It is reported as a diagnostic with race day excluded (now 1.27), and km monotony remains explicitly not a rule.
- Coach, minor: swim evening effectively forced to Monday and still unknown. ACCEPTED. Q5 asks for the evening and when he lifts; the answer becomes a weekday in AVAILABILITY. Until then, generated weeks use at most 4 evenings, leaving at least 3 free.
- Coach, minor: five runs is itself a step up; offer four. ACCEPTED in modified form. Q1 now says five exceeds his recorded maximum and offers four runs as 0/10/14/14/0/22/0, noting it needs an exception to the recovery-run-before-the-long-run rule. The coach's 0/10/14/0/14/22/0 was not adopted as written because it puts a 14 km run the day before the 22 km long run. Script check: any four-run 60 km week breaks either that rule or the first-run-after-race rule.
- Engineer, blocker: stored rows win, so the new generator never reaches the stored window. ACCEPTED. Stage 8 adds sessions.generator_version, stamped by planWeek. The daily pass regenerates planned rows with a stale or null version; done and skipped rows are untouched. The one-time loss of earlier repairs is stated and logged. Integration test specified.
- Engineer, blocker: multi-block and proposals have no consumer for DB weeks rows. ACCEPTED; Option A decided. The macro layer stays in config, with a BLOCKS array and season-unique week numbers so the weeks primary key and sessions foreign key hold. The seeder keeps referenced weeks, and the goal race is resolved per block. Proposal acceptance produces a config patch for a reviewed commit. Every call site is named. No REDLINES note is needed because no target lives outside config.
- Engineer, blocker: stage order contradicts its dependencies. ACCEPTED. New order: task and answers, persistence, config, structure check, roles, sizing, descriptions, guardrails, regeneration. Stages 1-8 merge together; stage 2 may merge alone. The declined-answer variants for Q1-Q3 (from variants.py) go into the frozen task header so the literals exist for either answer.
- Engineer, blocker: roles never survive the single plan reader. ACCEPTED as stage 1. role is added to PlannedSession and SessionRow, the store and memory mappers, both KINDS maps and their equality test, and diffWindows. A null role falls back to one inferred from kind, with a note. A round-trip test through planForDate is specified. The draft's claim that the planWeek signature was unchanged is corrected: the parameters are unchanged, the returned sessions gain a field.
- Engineer, major: a (date, time_slot) unique index breaks writes when a done row holds the slot. ACCEPTED. The migration dedupes planned duplicates first; replaceWindow runs in a transaction keyed by date|slot, and a planned session colliding with a non-planned row takes the free slot or is dropped with a note. Note for the record: nothing in src writes done or skipped today (grep), so the conflict is not reachable yet, but stage 9 introduces it.
- Engineer, major: doubles and the legs day need slot time-of-day data. ACCEPTED. AVAILABILITY.runSlots gains startLocal. toSessions honours a role's split (the double's first run in the earliest slot, the second in the latest) and the legs day's earliest slot. Precedence is stated: if the legs day's role minimum does not fit the earliest slot, legs moves to the next candidate, else it is dropped with a note.
- Engineer, major: the sizing formula cannot reproduce its own week 4. ACCEPTED. Section 4 is now numbered pseudo-code with explicit tie-breaks, re-applying the ratio after each cap and restarting when a run day reopens. gen2.py implements it, and the six-week table is its unedited output. Week 4 is now 0/17(10+7)/12/14/14/10/33; the medium Thursday (14) is at least as large as every other single-run day. Stage 5 asserts the full table.
- Engineer, major: post-race window has no data before stage 10 and an ambiguous definition. ACCEPTED. RACES[].effort moves into config at stage 2, with POST_RACE per effort. Raced: 1 easy day per 3 km. MP rehearsal: 4 days, so the 10K on day 7 falls outside Lincoln's window. Race day is day 0 and days 1..N are easy-only. Races and ratified long sessions are exempt, with MP zeroed inside windows. Tests cover the 10K and the day-7 long run. The marathon's window is evaluated by date and needs no next block.
- Engineer, major: the draft ignored the ratified week-2 days list. ADDRESSED by retiring it. decisions.md shows the planner authored it on 2026-09-06 and Luis never ratified it. Nothing in the planner reads it; only config tests 191-214, qualitySessionCount and the seeder's extra.days do, and those call sites are listed. A decision entry records the retirement, and Q1 covers the athlete-facing change, so no separate question.
- Engineer, major: V1-V4 blocking would refuse repairs and fire on every run. ACCEPTED. week-structure is advisory in evaluateGuardrails and enforced only as the generator self-test. Repairs keep roles: demote sets general-aerobic, and the spanner give-back skips recovery days and the day before a long session. Partial correction of the claim: scenarios.test.ts:61-73 and tools.test.ts compare against Object.keys dynamically, so they need no edit, but evaluateGuardrails must emit a result for both new ids or they fail.
- Engineer, major: numbers left undecided or defined twice. ACCEPTED. Long-run km lives only in config (20 and 13); the 60/40% is rationale plus the future proposeBlock template, not a runtime formula. Threshold minutes and rep length go in PRESCRIPTION with km derived from derivePaces, removing the 205 literal. sessions.prescription jsonb is dropped. Stride days go in config. The session-RPE monotony diagnostic is dropped. Medium-long reduces to one note number. The +/-4 marathon band literal in paces.ts moves to config. The field-claim test is widened to the new consts.
- Engineer, major: the strides rule contradicts the shakeout. ACCEPTED. Shakeout strides (4-6 x 80-100 m) are part of that role's dose, excluded from the add-on rule, and count as one of the week's two stride days. Race week gets Thu 22 plus the shakeout, and both cases are tested.
- Engineer, major: structure logic assumed one hard anchor per week. ACCEPTED. V3 and V4 are evaluated per anchor, and L+1 rests of every anchor are fixed first. The unused Pfitzinger tier table is deleted (minRunDays in config is the only input). The ladder only reopens rest days added by preference, never an L+1 rest. Weeks with two anchors (5 Oct and 19 Oct) are in the literal tests.
- Engineer, minor: tests that pass before any change. ACCEPTED. The 'km monotony 2.15 is not a breach' test is deleted. The week-2 note text is spelled out. The satisfies type and the foreign-field claim test are widened explicitly. The stage 7 reference to routr's invariant sweep is replaced by a manual delete-and-rerun step.
- Engineer, minor: factual errors about the code. ACCEPTED and corrected. diffWindows sums a day's sessions, so it merges doubles and hides slot moves; it does not lose them. toPlanned maps row by row. The real losses are store.ts replaceWindow and store-memory.ts, which also reuses one id for both sessions. The 10K is 4 days after Wed 7 Oct. gymFreeDaysBeforeRace (1 day) was checked; the new gap rule now governs legs.
- Self-correction: the draft's V1 threshold of 1.5 rested on a wrong claim ('lowest confirmed non-Daniels template 1.52'). Measured across all 79 non-race transcription weeks: Pfitzinger never below 1.6, Higdon Intermediate 2 step-back weeks 1.2, Hansons base weeks 1.0-1.33, bottom tenth 1.43. V1 is set to 1.4 as a labelled judgement. At 1.5 the 5 Oct week would need a double two days after Lincoln.
- Self-correction: the draft said 18 km six days out was 'heavier than every published taper'. That is wrong for distance: Pfitzinger 18/70 runs a 13 mi medium-long run 7 days out. Only the MP content was heavy, and Q2 is reframed that way, with the 18 km all-easy variant shown.
- Self-correction: 'first run after a race is recovery' now applies only to raced efforts. Applied to Lincoln, it forced Tue 6 Oct into a recovery double. After an MP rehearsal, the easy-only days carry the protection instead.
- Added from the data: two days before a raced effort is recovery or rest (Pfitzinger 3 of 3 tune-up weeks). The medium day's tie-break prefers the day after a hard session over the day before (Pfitzinger 18/70 medium-long runs follow quality in 7 of 12). The taper quality type is marathon pace (Higdon and Hansons taper pace runs).
- Engineering addition: race-run-in exempts a race-carrying session only up to the race distance, matching the 2026-09-07 spike decision. Without it, the 20 km wrapped around the 10K would be invisible to the run-in rule.
