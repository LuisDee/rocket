# CI gate status

Every gate this project should have, whether or not it exists yet. A needed gate
that is not built is a row marked `NOT IMPLEMENTED`, never an unlisted gap.

Update this table in the same commit that adds, removes, or fixes a gate. A row
more than one merge request stale is itself a finding.

## Rules this table obeys (enforced by `scripts/check_gate_ledger.py`)

- Status is one of: `IMPLEMENTED`, `NOT IMPLEMENTED`, `REJECTED`, `DISABLED`.
  A qualifier may follow: `IMPLEMENTED (local only)`, `DISABLED (was silently broken)`.
- An `IMPLEMENTED` row **must** carry `Enforced by:` naming one of:
  - a repo-relative path that exists (`scripts/foo/check.sh`)
  - `ci:<job-name>` that exists in the CI config
  - `hook:<hook-id>` that exists in `.pre-commit-config.yaml`
  - `external:<description>` for things outside the repo
- A `NOT IMPLEMENTED`, `REJECTED` or `DISABLED` row **must** carry `Reason:`.

The point of the mechanism field: a status is a promise about mechanism. Without
it, "IMPLEMENTED" degrades into "someone checked once", which is how a gate ends
up green for months while enforcing nothing.

## Evidence convention

For each gate, record **how you proved it fails**, not just that it passes. A
gate nobody watched fail is a decoration.

## Local/CI split

There is no local mirror of the CI jobs, by design. The only local hook is
`pre-push`, running task-trace, typecheck and tests -- the three worth catching
before a wasted deploy. Nothing fires on `git commit`. `check_hook_parity.py` was
removed rather than satisfied; see its `REJECTED` row.

## The table

| Gate                                                               | Status                     | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------------------------------ | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Type check (`tsc --noEmit`, strict + 4 extra flags)                | IMPLEMENTED                | Enforced by: ci:typecheck. Proven against a deliberate violation (assigned a string to a `number`), caught, reverted. Also runs on pre-push.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Lint (type-aware: floating promises, await-thenable, no-any)       | IMPLEMENTED                | Enforced by: ci:lint. Proven against a deliberate violation (unawaited `pull()` in a void function), caught, reverted. **Negative result:** stock `eslint-config-next` is not type-aware and passed the same probe; `typescript-eslint` with `projectService` was added for it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Formatter enforced in CI (`--check`, not auto-fix)                 | IMPLEMENTED                | Enforced by: ci:format-check. Proven against a deliberate violation (unformatted object literal), caught, reverted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Unit test suite                                                    | IMPLEMENTED                | Enforced by: ci:test. Proven against a deliberate violation (week 3 target raised to 95 km, breaking the ramp-cap assertion), caught, reverted. Also runs on pre-push.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Secrets scan                                                       | IMPLEMENTED                | Enforced by: ci:secrets-scan. Gitleaks, SHA-pinned. NOT yet proven against a planted secret -- do so on the first push once a remote exists.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Commit-to-task traceability                                        | IMPLEMENTED                | Enforced by: ci:task-trace. Fails closed on pre-push via `.githooks/pre-push`. Proven by `scripts/test_guards.py` (5 deliberate violations incl. missing trailer, dangling task path, commit absent from its task file).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Gate-ledger honesty (this table names its own enforcers)           | IMPLEMENTED                | Enforced by: ci:gate-ledger. Proven by `scripts/test_guards.py` (5 deliberate violations incl. a cited CI job that does not exist and a status with no reason).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Guard self-test (each guard still catches its violation)           | IMPLEMENTED                | Enforced by: ci:guard-self-test. 12/12 green. A broken guard looks exactly like a clean repo, which is why this job exists.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Dependency pinning (lockfile committed, actions SHA-pinned)        | IMPLEMENTED                | Enforced by: ci:test. Every job runs `npm ci`, which fails on lockfile drift; every action in the workflow is pinned by commit SHA with a version comment. NOT yet proven against a deliberately drifted lockfile.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Node version parity (local, CI, Vercel)                            | IMPLEMENTED                | Enforced by: .nvmrc. Pinned to 24 with `engines.node`; Vercel supports 24.x/22.x/20.x only. Caught during setup -- the scaffold defaulted to Node 25, which Vercel cannot run.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Integration harness tests (Garmin MFA wiring, token round-trip)    | IMPLEMENTED                | Enforced by: ci:harness-tests. Garmin only. Proven against a deliberate violation: a `save_token` that does not overwrite a rotated token, caught, reverted. The Strava half was withdrawn with `tools/strava_probe/` on 2026-09-07 on ToS grounds (`docs/decisions.md`, "the Strava API is withdrawn"); its scope-substring probe went with it. The workflow line invoking it went with it, so the job runs Garmin only and passes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Garmin token rotation persisted (no credential login per run)      | IMPLEMENTED (harness only) | Enforced by: ci:harness-tests. The library never writes rotated tokens back in inline-JSON mode -- `client.dump()` is gated on `tokenstore_path`, which stays None for an inline token -- so the harness re-persists after every login. Missing this degrades silently to a credential login per run and trips Garmin's per-account 429. Not yet enforced on the production sync path, which does not exist.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Append-only training history (no edit/delete of a logged run)      | IMPLEMENTED                | Enforced by: src/db/migrations/0001_append_only_guards.sql. Two layers binding two different actors: a privilege revoke stops `app_rw` (42501) and an `ENABLE ALWAYS` trigger stops the table owner (23001), with a separate statement-level trigger for TRUNCATE, which the row-level one does not fire on. Asserted on SQLSTATE, never on message text, by `src/db/guards.integration.test.ts` (ci:integration), and every case inserts a row first -- a row-level trigger does not fire on an UPDATE matching nothing, so the same assertions against an empty table would pass while proving nothing. Proven against four deliberate violations on the live Neon database, each reverted: disabled `activities_append_only` (owner UPDATE went from 23001 to no-error, and the tgenabled assertion caught `D`); disabled `activities_no_truncate` (TRUNCATE went from 23001 to no-error); granted UPDATE on activities to `app_rw` (42501 became 23001 -- the trigger still held, which is why the test asserts the privilege code specifically); revoked UPDATE/DELETE on notes from `app_rw` (the correct-and-withdraw test failed with permission denied). **Known limit, verified not assumed:** the Neon table owner CAN bypass by `ALTER TABLE ... DISABLE TRIGGER`, which succeeded and updated a row. Neon has no true superuser and the owner is refused `session_replication_role` (42501), but it owns the table. The guard therefore binds `app_rw` absolutely and the owner only against accident; `tgenabled = 'A'` is what catches a trigger left disabled afterwards, and a characterisation test records the bypass so a future Neon change is news. |
| Garmin rate-limit guard (a lockout is structurally impossible)     | IMPLEMENTED                | Enforced by: tools/garmin_probe/garmin_guard.py. Two buckets: login 2/day at 900s spacing with a 72h breaker, data 20/min and 600/day at 2s with a 30min breaker; reservation happens before the request and the breaker lives in sqlite, so it survives a restart. Proven against five deliberate violations, each reverted: removed the reserve-before-yield (a crashing call stopped costing budget); forced the breaker check to False (a tripped breaker stopped blocking a new process); removed the 429 branch in `run_call` (a 429 stopped aborting the sweep); made the corrupt-ledger path return instead of raise (fail-open); and neutered the no-credentials assert (the library's credential cascade became reachable again). **Numbers are safety margins from field reports, not published thresholds — no one has published Garmin's actual limits. A 429 is new information, never something to probe deliberately.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Garmin sync heartbeat (a stale pull is loud, not silent)           | NOT IMPLEMENTED            | Reason: no sync job yet. REDLINES.md rule 3. Design settled 2026-08-18 -- three layers, because each catches what the one above structurally cannot: (1) a `sync_run` row written under autocommit so the failure row survives the throw; (2) a healthchecks.io dead-man's switch pinged as the job's final step, the only layer that can detect the job never ran at all; (3) `get_status` reporting staleness once `SYNC.staleAfterHours` (36, already in `config/training.ts`) trips, with readiness renormalising to subjective-only weights. Owner: the M3 Garmin task, source decided by Decision gate G1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| CTL warm-up disclosure on readiness verdicts                       | IMPLEMENTED                | Enforced by: `src/domain/readiness.test.ts`. REDLINES.md rule 4. `scoreReadiness()` carries `insufficientHistory` and a `caveat` sentence naming the days it has against `LOAD.ctlWarmUpDays`, and `rocket_daily_checkin` returns both in its payload -- a caller cannot drop the disclosure without deleting a field. Scoped to the SUBJECTIVE score, which is the only readiness that exists today; the objective half and the load engine are still absent. Proved it fails: deleted the `/ totalWeight` renormalisation in `scoreReadiness` (2026-09-07) -- four assertions went red, including the band boundaries. The disclosure itself was separately checked by moving `historyDays` to `LOAD.ctlWarmUpDays`, which correctly drops the caveat.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Aggregates computed from the full series, with their window stated | NOT IMPLEMENTED            | Reason: the computed-aggregates redline is prose only -- no aggregate helper exists yet, and no check can see a model eyeballing an activity list. Candidate mechanism once the helper lands: it returns the coverage window alongside the value, and a guard forbids a call site discarding it. Live instance today: `src/data/recent-activities.json` is a 30-row snapshot carrying no declaration of the window it covers, and that boundary had to be established by hand before the review's ramp ratios could be computed from it. Owner: the M1 load-engine task.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Every guardrail in the config is actually evaluated by the planner | IMPLEMENTED                | Enforced by: src/domain/planner/guardrails.test.ts. `evaluateGuardrails()` returns at least one structured result for EVERY id in `GUARDRAIL_RULE_IDS` on every call, and the test asserts the returned id set equals the config's id set -- so a guardrail added to `config/training.ts` without an evaluator fails the suite rather than sitting in the registry unenforced. This is what makes `applied_rules[]` mean "consulted" rather than "happened to fire": without it, a planner that skipped the ramp cap entirely and one that checked and passed it return the identical envelope (`docs/specs/04-mcp-surface.md:43`). Proven against four deliberate violations, each reverted: dropped `sorenessRule` from the evaluator list (4 tests failed, the id-set assertion among them); narrowed `propose()` to validate only the edited session instead of the window (3 failed, including the ten-adjustments walk past the ramp cap); widened the soreness repair to touch every session (the swim-untouched assertion failed); hardcoded `shortfallKm = 0` (the unrunnable-week assertion failed). **Known limit:** the test proves each id is evaluated, not that its evaluation is correct -- correctness is what the neighbouring per-rule tests in the same file are for.                                                                                                                                                                                                                                                                                                                                                                                 |
| Session and week rows written only by the deterministic planner    | IMPLEMENTED                | Enforced by: `src/mcp/tools.test.ts`. REDLINES.md rule 8. Reads the PUBLISHED tool schemas over `tools/list`, so a tool added anywhere is covered: no parameter may be named `sessions`, `plan`, `schedule`, `week`, `weeks`, `days` or `window`, and no parameter may be an array of dated objects whatever it is called -- shape as well as naming, because a schedule renamed is still a schedule. A second assertion pins `session_id` to `rocket_adjust_session` alone, which takes one id and one typed field. Proved it fails: gave `rocket_add_note` a `sessions: {date, km}[]` parameter (2026-09-07) -- went red naming the tool and the parameter, reverted. Note the first attempt at this proof was a silent no-op, because the injected string did not match after formatting; the violation was re-injected against a verified marker and watched to fail.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| MCP endpoint refuses an unauthenticated request (fail-closed)      | IMPLEMENTED                | Enforced by: `src/app/api/mcp/route.test.ts`. The endpoint is an internet-reachable write surface into an append-only history, so the 401 assertions run UNCONDITIONALLY and come first -- a skipped auth test is an unarmed gate that makes every positive test behind it vacuous. Covers: no credential, wrong bearer, a bearer that is a prefix of the real one (constant-time compare), the secret unprovisioned, and that no `WWW-Authenticate` is advertised for an OAuth service this app does not host. Proved it fails: flipped `isAuthorised`'s unprovisioned branch from `return false` to `return true` (2026-09-07) -- "fails closed when the secret is not provisioned" went red.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| MCP tool input is validated and never throws at the transport      | IMPLEMENTED                | Enforced by: `src/mcp/tools.test.ts`. A thrown handler is a transport failure the model cannot recover from; `isError: true` with field-level text lets it correct itself and call again on the same connection, which the test asserts by making a second successful call after a rejected one. Driven through `tools/call` over a real client rather than by invoking handlers, so schema generation from the plain zod shapes is on the path -- calling handlers directly would not catch an `inputSchema` wrapped in `z.object()`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| No transitive zod 3.x under the MCP stack                          | NOT IMPLEMENTED            | Reason: verified by hand on 2026-09-07 (`npm ls zod` resolves every path to 4.4.3, deduped) but no CI step asserts it. A zod-3 copy dragged in transitively does not fail a build; it fails `tools/list` in front of a live connector, which is the worst place to find out. Candidate mechanism: one `npm ls zod` step in the `test` job. Owner: whoever next touches `.github/workflows/check.yml` -- deliberately not taken here, because three agents were editing this worktree and a shared CI file is the wrong thing to race on.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| No decision reads TSB (ATL, CTL and TSB are trend displays only)   | NOT IMPLEMENTED            | Reason: no load engine, so there is no TSB to read and nothing to gate on it. The 7/42 constants are conventions, not fitted parameters (Hellard 2006, Vermeire 2022), so a threshold on them has no calibrated meaning. Decisions come from the single-session cap, the weekly ramp cap, the soreness gate and readiness. Candidate mechanism: a grep guard forbidding `tsb` and `ctl` outside display code. Owner: the M1 load-engine task.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| No hardcoded training threshold outside `config/training.ts`       | NOT IMPLEMENTED            | Reason: REDLINES.md rule 1 is prose only today -- nothing stops a literal `0.15` appearing in a route handler. **The stated trigger has now fired** (2026-09-07): the planner grew past one file into `src/domain/planner/`. The obvious mechanism was tried and REJECTED on evidence rather than skipped: eslint's built-in `no-magic-numbers`, scoped to `src/domain/**` with `ignore: [0, 1, -1]` and array indexes allowed, reports 36 errors across the current domain code and not one of them is a training threshold -- it flags 7 (days in a week), 0.05 (a float epsilon), 1000 and 86_400_000 (millisecond conversions) and the weekday list `[1,2,3,4,5]` in a slot filter. The rule cannot tell a cap from a calendar, so satisfying it means naming a dozen structural constants for no safety gain, which is the shape of a gate people learn to silence. What would actually work: a guard that greps `src/domain/` for numeric literals appearing in a COMPARISON against a measured value (`> 15`, `>= 0.7`), which is where a threshold hides, rather than every literal. Not built here -- it is a repo-wide lint concern and the current diff is a planner. Owner: the next session touching lint infrastructure.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Architectural boundary (MCP tools and PWA share one core)          | NOT IMPLEMENTED            | Reason: only one consumer exists, so the rule would be vacuous today. Candidate: `dependency-cruiser`, one forbidden rule. Trigger: the first time an adaptation bug needs the same fix in two places.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Dependency vulnerability scan                                      | NOT IMPLEMENTED            | Reason: pinning is not scanning -- a pinned version can still be a known-vulnerable one. Wanted as a weekly scheduled job, not per-commit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Dependency update path (Renovate/Dependabot)                       | NOT IMPLEMENTED            | Reason: pins with no update path rot. Wanted once the repo has a remote.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Integration tests actually run in CI (no silent skip)              | IMPLEMENTED                | Enforced by: ci:integration. Runs the real migrations against a digest-pinned `postgres@sha256:4ef4dbc9...` (postgres:18) service, then `npm run test:integration`. The kit's own example tag-pins its service image, contradicting its own rule; that part was not copied. There is no `skipIf` in the suite: `src/db/integration-setup.ts` is a `globalSetup` that THROWS when DATABASE_URL is absent. Proven by running `test:integration` with DATABASE_URL unset -- exit 1, no tests reported -- and by running the whole flow (migrate, seed, 17 tests) against a local container on the pinned digest before the job was written.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Every guardrail in the config is actually evaluated                | IMPLEMENTED                | Enforced by: src/domain/planner/guardrails.test.ts. `evaluateGuardrails()` returns at least one structured result for every id in `GUARDRAIL_RULE_IDS` on every call, so `applied_rules[]` means EVALUATED rather than FIRED -- a planner that skipped the ramp cap entirely and one that checked and passed it are otherwise indistinguishable in the envelope (`docs/specs/04-mcp-surface.md:43`). Proven by deleting `sorenessRule` from the evaluation list on 2026-09-07: three tests failed, the coverage test naming the missing id. Restored. The gate also resists being satisfied without the work -- returning a stub result for an id would pass the coverage test but fail the per-rule tests beside it, which assert the threshold and the observed value.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Guardrail baselines come from completed runs, not from the plan    | IMPLEMENTED                | Enforced by: src/mcp/tools.test.ts. Every tool that evaluates the window passes `store.completedRuns()` over the trailing `GUARDRAILS.singleSessionSpikeWindowDays`, so the ramp cap measures a planned week against a RUN week. Without it the rule compares plan to plan and always passes, because the plan was authored to pass it -- enforced-looking and enforcing nothing. Proven by replacing the history fetch with an empty array on 2026-09-07: three tests failed, including the spike warning. Restored.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| No Strava API call anywhere in the product tree                    | IMPLEMENTED                | Enforced by: scripts/check_no_strava_api.py. Wired into ci:no-strava-api and into `npm run guards`. Strava API Policy 2026 section 5.3 bars using Strava API Materials to operate an AI Application, and section 3.5 makes the Strava MCP the sole authorised agent-mediated interface -- so rocket reads through the MCP connector and writes nothing at all (`docs/decisions.md`, "Strava uploads are prohibited; the pipeline ends at the preview"). The guard matches a Strava hostname, a Strava OAuth credential name, and the withdrawn `tools/strava_probe` path, across `src/`, `config/`, `tools/`, `scripts/`, `.github/` and `.env.example`. It deliberately does NOT match the word "Strava", `strava_activity_id` or `stravaActivityId` -- a guard that fired on the word would be switched off within a day, and the tree contains all three today while the guard passes. Proven against three deliberate violations, all reverted: a `src/lib/strava.ts` posting a FIT file to `https://www.strava.com/api/v3/uploads` (caught at line 2, exit 1) -- which is precisely what `tasks/preview-and-ship.md` scoped; `STRAVA_CLIENT_SECRET` appended to `.env.example` (caught); and a recreated `tools/strava_probe/` directory (caught by the path check, which the line scan alone would have missed on an empty file). Clean tree before and after: 862 files scanned, exit 0. **Known limit:** it cannot catch a hostname assembled at runtime from fragments, and it does not police what the Strava MCP connector itself returns -- section 5.5 (no Strava Data in a Persistent Index) binds the MCP path too and is NOT mechanically checked.        |
| Default-branch protection (no direct push)                         | NOT IMPLEMENTED            | Reason: no remote exists yet. Record as `external:` once GitHub is wired. Low value while there is exactly one committer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Test coverage floor                                                | REJECTED                   | Reason: a percentage floor measures lines executed, not behaviour asserted, and is trivially satisfied by bare imports. Replaced by the rule that every gate must be proven to fail. Revisit only if untested paths start biting.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Local/CI hook parity                                               | REJECTED                   | Reason: `check_hook_parity.py` reads local hook ids only from `.pre-commit-config.yaml` (`_config_readers.py:52-56`). This repo deliberately has no pre-commit framework, and installing a Python hook runner in a Node project to satisfy a parity check is the tail wagging the dog. Guard removed rather than left failing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Structure-doc coverage (README per tracked directory)              | REJECTED                   | Reason: a repo this size fits in one head, and the guard fails on a fresh install because `docs/` gets files but no `docs/README.md`. Guard and its `collect_structure_docs.py` companion removed. Re-add when the layout stops being memorable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Container image scan + non-root user                               | REJECTED                   | Reason: no containers. Deploys to Vercel; the only heavy container in the wider system is routr's GraphHopper, which lives in a separate repo.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Cross-model pre-push review                                        | REJECTED                   | Reason: ds-lestrade's `gemini-review.sh` fails open, needs an external binary, and costs latency on every push. On a solo project the second model is already in the loop driving the work.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
