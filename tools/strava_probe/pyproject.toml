[project]
name = "rocket-strava-probe"
version = "0.1.0"
description = "One-time Strava OAuth exchange for rocket."
requires-python = ">=3.12"
# stdlib only: this is one HTTPS POST. A dependency would be more code than the script.
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]
